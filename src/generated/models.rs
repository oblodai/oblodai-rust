// Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.
//! Models of the API: plain structs, (de)serialized with serde.
//!
//! Fields this SDK version does not know yet land in `extra`; an unknown enum value parses to the
//! enum's `Other`; a `oneOf` shape it does not know parses to the union's `Unknown`.

use std::collections::BTreeMap;

// --- runtime imports: the only names taken from the hand-written runtime; adjust here ---
use crate::core::debug::ModelDebug;
use crate::core::money::Money;

use super::enums::{
    AcceptedReason, AmountMode, AssetKind, AutoConvertMode, BatchItemStatus, BatchKind,
    BatchOnError, BatchStatus, ConversionWebhookStatus, DocumentJobKind, DocumentJobStatus,
    FeeType, OnrampIdleStatus, OnrampStatus, PaymentStatus, PayoutFeeBearer, PayoutKind,
    PayoutLinkFeeBearer, PayoutLinkStatus, PayoutSource, PayoutStatus, RefundRollup, SoFStatus,
    WebhookDeliveryStatus,
};

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AMLLinkView {
    /// До какого момента ссылка действует (UTC).
    pub expired_at: String,
    /// Ссылка на анкету — передайте её плательщику.
    pub link: String,
    /// Статус заполнения анкеты.
    pub status: SoFStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AMLLinkView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AMLLinkView");
        d.field("expired_at", &self.expired_at);
        d.field("link", &self.link);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AMLLinksRequest {
    /// Идентификатор заказа мерчанта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Идентификатор платежа. Нужен uuid или order_id; приоритет у uuid.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AMLLinksRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AMLLinksRequest");
        d.field("order_id", &self.order_id);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AMLLinksResult {
    /// По ссылке на каждый заблокированный депозит платежа; пусто — блокировать нечего.
    pub items: Vec<AMLLinkView>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AMLLinksResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AMLLinksResult");
        d.field("items", &self.items);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct APIAllowEnableRequest {
    /// true — принимать API-вызовы только с адресов из списка; false — список хранится, но не
    /// применяется.
    pub enabled: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for APIAllowEnableRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "APIAllowEnableRequest");
        d.field("enabled", &self.enabled);
        d.extra(&self.extra);
        d.finish()
    }
}

impl APIAllowEnableRequest {
    /// A `APIAllowEnableRequest` with the required fields; the optional ones start as `None`.
    pub fn new(enabled: bool) -> Self {
        Self {
            enabled,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct APIAllowEntryRequest {
    /// IP или подсеть в CIDR (203.0.113.7 или 203.0.113.0/24).
    pub cidr: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for APIAllowEntryRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "APIAllowEntryRequest");
        d.field("cidr", &self.cidr);
        d.extra(&self.extra);
        d.finish()
    }
}

impl APIAllowEntryRequest {
    /// A `APIAllowEntryRequest` with the required fields; the optional ones start as `None`.
    pub fn new(cidr: impl Into<String>) -> Self {
        Self {
            cidr: cidr.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct APIAllowListResult {
    /// Применяется ли список: true — вызовы с адресов вне списка получают 403 auth.ip_not_allowed.
    pub enabled: bool,
    /// Разрешённые IP и подсети в CIDR.
    pub items: Vec<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for APIAllowListResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "APIAllowListResult");
        d.field("enabled", &self.enabled);
        d.field("items", &self.items);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct APILogEntry {
    /// Ключ, которым подписан запрос.
    pub api_key_id: String,
    /// Когда пришёл запрос (UTC).
    pub created_at: String,
    /// Длительность обработки, мс.
    pub duration_ms: i64,
    /// Адрес клиента.
    pub ip: String,
    /// HTTP-метод.
    pub method: String,
    /// Путь запроса.
    pub path: String,
    /// Код ответа.
    pub status: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for APILogEntry {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "APILogEntry");
        d.field("api_key_id", &self.api_key_id);
        d.field("created_at", &self.created_at);
        d.field("duration_ms", &self.duration_ms);
        d.field("ip", &self.ip);
        d.field("method", &self.method);
        d.field("path", &self.path);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct APILogRequest {
    /// Начало периода, YYYY-MM-DD, включительно.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from: Option<String>,
    /// Размер страницы, 1..200; по умолчанию 20.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Страница, с 1.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub page: Option<i64>,
    /// Подстрока по «МЕТОД путь» — то, что человек видит в таблице.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub q: Option<String>,
    /// Точный код ответа; 0 — все.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<i64>,
    /// Конец периода, YYYY-MM-DD, ВКЛЮЧИТЕЛЬНО (день целиком).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub to: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for APILogRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "APILogRequest");
        d.field("from", &self.from);
        d.field("limit", &self.limit);
        d.field("page", &self.page);
        d.field("q", &self.q);
        d.field("status", &self.status);
        d.field("to", &self.to);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct APILogResult {
    /// Строки этой страницы, новые сверху.
    pub items: Vec<APILogEntry>,
    /// Сколько дней лог хранится.
    pub retention_days: i64,
    /// Всего строк по фильтру.
    pub total: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for APILogResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "APILogResult");
        d.field("items", &self.items);
        d.field("retention_days", &self.retention_days);
        d.field("total", &self.total);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AcceptedConfiguredMethod {
    /// Можно ли платить этим методом здесь.
    pub available: bool,
    /// Код актива.
    pub currency: String,
    /// Сеть актива.
    pub network: String,
    /// Почему недоступен: not_served_here — развёртывание не принимает этот метод, unknown_method —
    /// метода нет в каталоге; у доступного ключа нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reason: Option<AcceptedReason>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AcceptedConfiguredMethod {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AcceptedConfiguredMethod");
        d.field("available", &self.available);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("reason", &self.reason);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AcceptedConfiguredMethodList {
    /// Записи этой страницы.
    pub items: Vec<AcceptedConfiguredMethod>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AcceptedConfiguredMethodList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AcceptedConfiguredMethodList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AcceptedMethod {
    /// Код актива.
    pub currency: String,
    /// Сеть актива.
    pub network: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AcceptedMethod {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AcceptedMethod");
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AcceptedSetRequest {
    /// Полный список пар валюта+сеть, которыми разрешено платить; пустой список — принимать всё из
    /// каталога.
    pub accepted: Vec<AcceptedMethod>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AcceptedSetRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AcceptedSetRequest");
        d.field("accepted", &self.accepted);
        d.extra(&self.extra);
        d.finish()
    }
}

impl AcceptedSetRequest {
    /// A `AcceptedSetRequest` with the required fields; the optional ones start as `None`.
    pub fn new(accepted: Vec<AcceptedMethod>) -> Self {
        Self {
            accepted,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AcceptedSetResult {
    /// Набор сохранён.
    pub ok: bool,
    /// Сохранённые, но неизвестные каталогу пары — оставлены как были; ключа нет, когда таких нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub unknown: Option<Vec<AcceptedMethod>>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AcceptedSetResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AcceptedSetResult");
        d.field("ok", &self.ok);
        d.field("unknown", &self.unknown);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AccuracyResult {
    /// Допуск в процентах, 1–5; 0 — допуск выключен (нужна точная сумма).
    pub accuracy_percent: i64,
    /// Включён ли допуск.
    pub enabled: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AccuracyResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AccuracyResult");
        d.field("accuracy_percent", &self.accuracy_percent);
        d.field("enabled", &self.enabled);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ApproveRequest {
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ApproveRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ApproveRequest");
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

impl ApproveRequest {
    /// A `ApproveRequest` with the required fields; the optional ones start as `None`.
    pub fn new(uuid: impl Into<String>) -> Self {
        Self {
            uuid: uuid.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AutoConvertResult {
    /// Есть ли приказ. false — остальные поля — умолчания формы.
    pub configured: bool,
    /// Включён ли приказ.
    pub enabled: bool,
    /// Пол одной конвертации в долларах, десятичной строкой (с умолчанием процесса).
    pub min_amount: Money,
    /// Режим зачисления: economy или instant.
    pub mode: AutoConvertMode,
    /// Монеты, которые сводятся; пусто — \[\], не null.
    pub sources: Vec<String>,
    /// Монета, в которую сводится выручка; пусто без приказа.
    pub target: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AutoConvertResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AutoConvertResult");
        d.field("configured", &self.configured);
        d.field("enabled", &self.enabled);
        d.field("min_amount", &self.min_amount);
        d.field("mode", &self.mode);
        d.field("sources", &self.sources);
        d.field("target", &self.target);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AutoRefundPolicyResult {
    /// false — политику не задавали, действует умолчание (обе включены).
    pub configured: bool,
    /// Возвращается ли излишек при переплате (paid_over).
    pub overpay: bool,
    /// Возвращаются ли средства при истёкшей недоплате (wrong_amount).
    pub underpay: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AutoRefundPolicyResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AutoRefundPolicyResult");
        d.field("configured", &self.configured);
        d.field("overpay", &self.overpay);
        d.field("underpay", &self.underpay);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AutoWithdrawDeleteRequest {
    /// Актив, автовывод которого выключить.
    pub currency: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AutoWithdrawDeleteRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AutoWithdrawDeleteRequest");
        d.field("currency", &self.currency);
        d.extra(&self.extra);
        d.finish()
    }
}

impl AutoWithdrawDeleteRequest {
    /// A `AutoWithdrawDeleteRequest` with the required fields; the optional ones start as `None`.
    pub fn new(currency: impl Into<String>) -> Self {
        Self {
            currency: currency.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AutoWithdrawListResult {
    /// Правила автовывода, по одному на актив.
    pub items: Vec<AutoWithdrawRule>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AutoWithdrawListResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AutoWithdrawListResult");
        d.field("items", &self.items);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AutoWithdrawRule {
    /// Адрес назначения.
    pub address: String,
    /// Актив.
    pub currency: String,
    /// Порог срабатывания в единицах актива.
    pub min_amount: Money,
    /// Сеть адреса назначения.
    pub network: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AutoWithdrawRule {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AutoWithdrawRule");
        d.field("address", &self.address);
        d.field("currency", &self.currency);
        d.field("min_amount", &self.min_amount);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct AutoWithdrawSetRequest {
    /// Адрес назначения (внешний кошелёк мерчанта).
    pub address: String,
    /// Актив, который выводить автоматически.
    pub currency: String,
    /// Сеть адреса назначения.
    pub network: String,
    /// Порог: вывод срабатывает, когда доступный баланс актива не меньше этой суммы; пусто —
    /// сетевой минимум.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for AutoWithdrawSetRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "AutoWithdrawSetRequest");
        d.field("address", &self.address);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("min_amount", &self.min_amount);
        d.extra(&self.extra);
        d.finish()
    }
}

impl AutoWithdrawSetRequest {
    /// A `AutoWithdrawSetRequest` with the required fields; the optional ones start as `None`.
    pub fn new(
        address: impl Into<String>,
        currency: impl Into<String>,
        network: impl Into<String>,
    ) -> Self {
        Self {
            address: address.into(),
            currency: currency.into(),
            network: network.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BalanceResult {
    /// Балансы владельца.
    pub balance: MerchantBalances,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BalanceResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BalanceResult");
        d.field("balance", &self.balance);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BatchInfoItem {
    /// Порядковый номер элемента в исходном массиве (с нуля).
    pub idx: i64,
    /// Статус элемента: pending | processing | done | error.
    pub status: BatchItemStatus,
    /// Машиночитаемый код ошибки — тот же, что вернул бы одиночный вызов (payment.below_minimum,
    /// payout.address_network_mismatch, …); batch.stopped / batch.key_revoked — элемент не
    /// выполнялся; только при status «error». Пусто у элементов, завершённых до ввода поля.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_code: Option<String>,
    /// HTTP-статус, которым ответил бы одиночный вызов (400, 409, …); отсутствует, если элемент не
    /// дошёл до обработчика (batch.stopped, batch.key_revoked).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub http_status: Option<i64>,
    /// Человекочитаемое сообщение об ошибке; только при status «error».
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Итог элемента: true при status «done», false при status «error»; отсутствует, пока элемент
    /// не обработан.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ok: Option<bool>,
    /// order_id элемента, если вы его задавали; присутствует не всегда.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Результат успешной операции — тот же объект, что вернул бы одиночный вызов; только при
    /// status «done».
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<serde_json::Value>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BatchInfoItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BatchInfoItem");
        d.field("idx", &self.idx);
        d.field("status", &self.status);
        d.field("error_code", &self.error_code);
        d.field("http_status", &self.http_status);
        d.field("message", &self.message);
        d.field("ok", &self.ok);
        d.field("order_id", &self.order_id);
        d.field("result", &self.result);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BatchInfoRequest {
    /// Идентификатор батча из ответа на submit.
    pub batch_id: String,
    /// Сколько элементов вернуть в items (пагинация).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Смещение по элементам.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub offset: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BatchInfoRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BatchInfoRequest");
        d.field("batch_id", &self.batch_id);
        d.field("limit", &self.limit);
        d.field("offset", &self.offset);
        d.extra(&self.extra);
        d.finish()
    }
}

impl BatchInfoRequest {
    /// A `BatchInfoRequest` with the required fields; the optional ones start as `None`.
    pub fn new(batch_id: impl Into<String>) -> Self {
        Self {
            batch_id: batch_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BatchInfoResponse {
    /// Идентификатор батча.
    pub batch_id: String,
    /// Время создания батча (ISO 8601, UTC).
    pub created_at: String,
    /// Завершилось ошибкой (при on_error stop сюда попадают и пропущенные элементы).
    pub failed: i64,
    /// Страница элементов с результатом или ошибкой по каждому.
    pub items: Vec<BatchInfoItem>,
    /// Вид батча: payment | refund | payout | transfer.
    pub kind: BatchKind,
    /// Режим обработки ошибок, с которым батч был отправлен: continue | stop.
    pub on_error: BatchOnError,
    /// Статус батча: pending | processing | completed | stopped. ТЕРМИНАЛЬНЫЕ — completed И stopped
    /// (опрашивайте до одного из них, не только до completed): completed = обработка дошла до
    /// конца, stopped = батч с on_error=stop остановился на первой ошибке (остальные элементы
    /// пропущены и учтены в failed). Ни один не значит «всё успешно» — смотрите succeeded/failed.
    pub status: BatchStatus,
    /// Успешно обработано.
    pub succeeded: i64,
    /// Всего элементов в батче; считается по всему батчу и от пагинации не зависит.
    pub total: i64,
    /// Время последнего изменения (ISO 8601, UTC).
    pub updated_at: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BatchInfoResponse {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BatchInfoResponse");
        d.field("batch_id", &self.batch_id);
        d.field("created_at", &self.created_at);
        d.field("failed", &self.failed);
        d.field("items", &self.items);
        d.field("kind", &self.kind);
        d.field("on_error", &self.on_error);
        d.field("status", &self.status);
        d.field("succeeded", &self.succeeded);
        d.field("total", &self.total);
        d.field("updated_at", &self.updated_at);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BatchSubmitResponse {
    /// Идентификатор батча — с ним идите в POST /v1/batch/info за статусом и результатами.
    pub batch_id: String,
    /// Сколько элементов принято в обработку.
    pub count: i64,
    /// Вид батча: payment | refund | payout | transfer.
    pub kind: BatchKind,
    /// Стартовый статус — всегда pending.
    pub status: BatchStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BatchSubmitResponse {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BatchSubmitResponse");
        d.field("batch_id", &self.batch_id);
        d.field("count", &self.count);
        d.field("kind", &self.kind);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BlockWalletRequest {
    /// Адрес статического кошелька
    pub address: String,
    /// true — заблокировать (значение по умолчанию, если поле опущено); false — снять блокировку
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_force_block: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BlockWalletRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BlockWalletRequest");
        d.field("address", &self.address);
        d.field("is_force_block", &self.is_force_block);
        d.extra(&self.extra);
        d.finish()
    }
}

impl BlockWalletRequest {
    /// A `BlockWalletRequest` with the required fields; the optional ones start as `None`.
    pub fn new(address: impl Into<String>) -> Self {
        Self {
            address: address.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BlockWalletResult {
    /// Адрес кошелька.
    pub address: String,
    /// Заблокирован ли кошелёк после вызова.
    pub blocked: bool,
    /// Идентификатор статического кошелька.
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BlockWalletResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BlockWalletResult");
        d.field("address", &self.address);
        d.field("blocked", &self.blocked);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BlockedRefundRequest {
    /// Адрес назначения возврата.
    pub address: String,
    /// Идентификатор статического кошелька (из ответа /v1/wallet).
    pub uuid: String,
    /// Тег/мемо назначения (XRP destination tag, XLM memo id, TON comment). Обязателен для
    /// классического адреса на tag/memo-сети, если тег не встроен в X-/M-адрес.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub memo: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BlockedRefundRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BlockedRefundRequest");
        d.field("address", &self.address);
        d.field("uuid", &self.uuid);
        d.field("memo", &self.memo);
        d.extra(&self.extra);
        d.finish()
    }
}

impl BlockedRefundRequest {
    /// A `BlockedRefundRequest` with the required fields; the optional ones start as `None`.
    pub fn new(address: impl Into<String>, uuid: impl Into<String>) -> Self {
        Self {
            address: address.into(),
            uuid: uuid.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct BlockedRefundResult {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency, списанная с вашего баланса.
    pub amount: Money,
    /// true — выплата ждёт подтверждения (внутренние сценарии; по API-ключу всегда false).
    pub approval_required: bool,
    /// Удержанная сетевая комиссия, в валюте выплаты. 0 — комиссию поглотил шлюз.
    pub commission: Money,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Код валюты выплаты.
    pub currency: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать получателю. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Кто заплатил сетевую комиссию: gateway — шлюз поглотил её (commission = 0); merchant — сумма
    /// списания увеличена на комиссию, получатель получает запрошенное целиком (is_subtract=true,
    /// выплатная ссылка с fee_bearer=merchant); recipient — комиссия удержана из выплаты,
    /// получателю приходит меньше запрошенного.
    pub fee_bearer: PayoutFeeBearer,
    /// true — статус финальный (confirmed / failed / cancelled).
    pub is_final: bool,
    /// true — это возврат платежа, а не обычная выплата.
    pub is_refund: bool,
    /// Тег/мемо назначения, переданный при создании (TON Jetton, memo-биржи). Пусто — без мемо.
    pub memo: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Сколько реально уходит получателю на адрес: amount − commission.
    pub payer_amount: Money,
    /// api (через интеграцию) | manual (из кабинета).
    pub source: PayoutSource,
    /// Статус выплаты: pending (создана, ждёт) | approved (одобрена) | awaiting_cosign (ждёт второй
    /// подписи) | broadcasting (отправляется) | sent (отправлена, ждёт подтверждений) | confirmed
    /// (подтверждена — готово) | failed | cancelled. Значение можно передать обратно в фильтр
    /// истории как есть.
    pub status: PayoutStatus,
    /// Хеш транзакции в блокчейне (появляется после отправки).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Кошелёк, с которого вернули деньги.
    pub wallet_uuid: String,
    /// Ваш номер (reference) выплаты. У возврата — null: возврат не имеет вашего идентификатора,
    /// см. payment_order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Ваш order_id платежа, по которому сделан возврат (null у обычной выплаты). У возврата
    /// собственного order_id нет — он приходит null, а сверять возврат с заказом нужно по этому
    /// полю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payment_order_id: Option<String>,
    /// Идентификатор возвращаемого платежа (null, если это не возврат).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_for: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for BlockedRefundResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "BlockedRefundResult");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("approval_required", &self.approval_required);
        d.field("commission", &self.commission);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("is_final", &self.is_final);
        d.field("is_refund", &self.is_refund);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("source", &self.source);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("uuid", &self.uuid);
        d.field("wallet_uuid", &self.wallet_uuid);
        d.field("order_id", &self.order_id);
        d.field("payment_order_id", &self.payment_order_id);
        d.field("refund_for", &self.refund_for);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CancelPayoutRequest {
    /// Идентификатор выплаты (или возврата) для отмены.
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CancelPayoutRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CancelPayoutRequest");
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

impl CancelPayoutRequest {
    /// A `CancelPayoutRequest` with the required fields; the optional ones start as `None`.
    pub fn new(uuid: impl Into<String>) -> Self {
        Self {
            uuid: uuid.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CheckoutConfigRequest {
    /// Слать ли покупателю чек на почту после оплаты. Чек уходит только если покупатель оставил
    /// адрес. По умолчанию — да.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email_receipts: Option<bool>,
    /// Куда вернуть покупателя, если он ушёл с оплаты. Пустая строка — никуда не отправлять. Поле
    /// можно не присылать — тогда прежнее значение сохранится. Подставляется только в те счета, где
    /// url_return не задан.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fail_url: Option<String>,
    /// Куда вернуть покупателя после успешной оплаты. Пустая строка — никуда не отправлять. Поле
    /// можно не присылать — тогда прежнее значение сохранится. Подставляется только в те счета, где
    /// url_success не задан.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub success_url: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CheckoutConfigRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CheckoutConfigRequest");
        d.field("email_receipts", &self.email_receipts);
        d.field("fail_url", &self.fail_url);
        d.field("success_url", &self.success_url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CheckoutConfigView {
    /// Слать ли покупателю чек на почту после оплаты.
    pub email_receipts: bool,
    /// Куда вернуть покупателя, ушедшего с оплаты; пусто — никуда.
    pub fail_url: String,
    /// Куда вернуть покупателя после оплаты; пусто — никуда.
    pub success_url: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CheckoutConfigView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CheckoutConfigView");
        d.field("email_receipts", &self.email_receipts);
        d.field("fail_url", &self.fail_url);
        d.field("success_url", &self.success_url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ClaimRequest {
    /// Адрес получателя в сети выплаты.
    pub address: String,
    /// Memo/tag — только для сетей, где он обязателен.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub memo: Option<String>,
    /// Код получения — если отправитель установил его на ссылку. После 10 неверных вводов ссылка
    /// запирается.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub passcode: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ClaimRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ClaimRequest");
        d.field("address", &self.address);
        d.field("memo", &self.memo);
        d.field("passcode", &self.passcode);
        d.extra(&self.extra);
        d.finish()
    }
}

impl ClaimRequest {
    /// A `ClaimRequest` with the required fields; the optional ones start as `None`.
    pub fn new(address: impl Into<String>) -> Self {
        Self {
            address: address.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConversionEconomyQuote {
    /// Доступен ли режим сейчас.
    pub available: bool,
    /// Комиссия режима в процентах.
    pub fee_percent: Money,
    /// Гарантированный минимум к получению, в валюте котировки.
    pub min_out: Money,
    /// Почему недоступен: no_route; пусто — доступен.
    pub reason: String,
    /// За сколько минут исполняется заявка.
    pub window_minutes: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ConversionEconomyQuote {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ConversionEconomyQuote");
        d.field("available", &self.available);
        d.field("fee_percent", &self.fee_percent);
        d.field("min_out", &self.min_out);
        d.field("reason", &self.reason);
        d.field("window_minutes", &self.window_minutes);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConversionInstantQuote {
    /// Доступен ли режим сейчас.
    pub available: bool,
    /// Сколько придёт, в валюте котировки.
    pub estimated_out: Money,
    /// Комиссия режима в процентах.
    pub fee_percent: Money,
    /// Почему недоступен: frozen, position_cap; пусто — доступен.
    pub reason: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ConversionInstantQuote {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ConversionInstantQuote");
        d.field("available", &self.available);
        d.field("estimated_out", &self.estimated_out);
        d.field("fee_percent", &self.fee_percent);
        d.field("reason", &self.reason);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConversionModes {
    /// Конвертация через партию ликвидации.
    pub economy: ConversionEconomyQuote,
    /// Мгновенная конвертация по спред-курсу.
    pub instant: ConversionInstantQuote,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ConversionModes {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ConversionModes");
        d.field("economy", &self.economy);
        d.field("instant", &self.instant);
        d.extra(&self.extra);
        d.finish()
    }
}

/// Приходит, когда конвертация в эконом-режиме исполнена (completed — зачислено) или отменена с
/// возвратом исходной суммы (refunded).
#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ConversionWebhook {
    /// Когда завершена (ISO 8601).
    pub completed_at: String,
    /// Когда конвертация принята (ISO 8601).
    pub created_at: String,
    /// Подписанная ссылка на PDF-чек конвертации; пусто у возврата и когда документы выключены.
    pub document_url: String,
    /// Когда событие произошло, UTC с миллисекундами (ISO 8601).
    pub event_at: String,
    /// Комиссия конвертации, в процентах.
    pub fee_percent: Money,
    /// Из какой валюты.
    pub from: String,
    /// Идентификатор конвертации — тот id, что вернул запрос конвертации.
    pub id: String,
    /// Всегда true: событие приходит, когда деньги уже зачислены или возвращены.
    pub is_final: bool,
    /// Режим: economy (исполнена очередью) | instant.
    pub mode: String,
    /// Причина возврата (market_below_min | window_expired); пусто у completed.
    pub reason: String,
    /// Сколько отдано, в валюте from.
    pub sent: Money,
    /// Глобальный номер события: в пределах одного объекта больший номер новее, меньший —
    /// опоздавшая доставка, её нужно отбросить. У репетиции (test: true) всегда 0.
    pub sequence: i64,
    /// completed — зачислено; refunded — исходная сумма возвращена.
    pub status: ConversionWebhookStatus,
    /// В какую валюту.
    pub to: String,
    /// Вид события: payment | payout | wallet | conversion — какое тело пришло.
    pub r#type: String,
    /// Сколько зачислено, в валюте to. Есть только у completed; у refunded поля нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub received: Option<Money>,
    /// Есть только у репетиции (/v1/test-webhook/*, /v1/payment/testing-webhook) и всегда true —
    /// внутри подписи. Боевое событие этого поля не несёт никогда: тело с test: true обработчик
    /// обязан игнорировать, даже если подпись верна.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub test: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ConversionWebhook {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ConversionWebhook");
        d.field("completed_at", &self.completed_at);
        d.field("created_at", &self.created_at);
        d.field("document_url", &self.document_url);
        d.field("event_at", &self.event_at);
        d.field("fee_percent", &self.fee_percent);
        d.field("from", &self.from);
        d.field("id", &self.id);
        d.field("is_final", &self.is_final);
        d.field("mode", &self.mode);
        d.field("reason", &self.reason);
        d.field("sent", &self.sent);
        d.field("sequence", &self.sequence);
        d.field("status", &self.status);
        d.field("to", &self.to);
        d.field("type", &self.r#type);
        d.field("received", &self.received);
        d.field("test", &self.test);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CreateWalletRequest {
    /// Символ валюты приёма (USDT, BTC, ETH, …)
    pub currency: String,
    /// Сеть приёма (tron, ethereum, bitcoin, …)
    pub network: String,
    /// Ваш идентификатор клиента/заказа. Закрепляет отдельный постоянный адрес за клиентом
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CreateWalletRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CreateWalletRequest");
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl CreateWalletRequest {
    /// A `CreateWalletRequest` with the required fields; the optional ones start as `None`.
    pub fn new(currency: impl Into<String>, network: impl Into<String>) -> Self {
        Self {
            currency: currency.into(),
            network: network.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CurrenciesResult {
    /// Чем счёт можно оплатить: монеты по сетям.
    pub currencies: Vec<CurrencyEntry>,
    /// В чём счёт можно выставить: те же монеты и фиат; отсортированы по коду.
    pub pricing_currencies: Vec<PricingCurrency>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CurrenciesResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CurrenciesResult");
        d.field("currencies", &self.currencies);
        d.field("pricing_currencies", &self.pricing_currencies);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CurrencyEntry {
    /// Код валюты.
    pub currency: String,
    /// Знаков после запятой в суммах этой валюты.
    pub decimals: i64,
    pub networks: Vec<CurrencyNetwork>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CurrencyEntry {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CurrencyEntry");
        d.field("currency", &self.currency);
        d.field("decimals", &self.decimals);
        d.field("networks", &self.networks);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct CurrencyNetwork {
    /// То же, что deposit_available.
    pub available: bool,
    /// false — метод показывается на оплате только после явного включения мерчантом.
    pub default_offer: bool,
    /// Приём в этой сети работает на этом развёртывании.
    pub deposit_available: bool,
    /// native — монета сети, token — токен контракта.
    pub kind: AssetKind,
    /// Подтверждений до зачисления.
    pub min_confirmations: i64,
    /// Сеть.
    pub network: String,
    /// Выплаты в этой сети работают на этом развёртывании.
    pub payout_available: bool,
    /// Номер EVM-сети (EIP-155); только у EVM-сетей.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub chain_id: Option<i64>,
    /// Контракт токена; у монеты сети ключа нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub contract: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for CurrencyNetwork {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "CurrencyNetwork");
        d.field("available", &self.available);
        d.field("default_offer", &self.default_offer);
        d.field("deposit_available", &self.deposit_available);
        d.field("kind", &self.kind);
        d.field("min_confirmations", &self.min_confirmations);
        d.field("network", &self.network);
        d.field("payout_available", &self.payout_available);
        d.field("chain_id", &self.chain_id);
        d.field("contract", &self.contract);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobAccepted {
    /// Когда задача поставлена (UTC).
    pub created_at: String,
    /// Формат файла: pdf или csv.
    pub format: String,
    /// Идентификатор задачи.
    pub job_id: String,
    /// Вид отчёта.
    pub kind: DocumentJobKind,
    /// Язык документа.
    pub lang: String,
    /// Период отчёта.
    pub period: DocumentJobPeriod,
    /// Статус задачи: queued, processing, done, failed или expired.
    pub status: DocumentJobStatus,
    /// Когда задача менялась последний раз (UTC).
    pub updated_at: String,
    /// Почему файла нет; есть у задачи в статусе failed или expired.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<DocumentJobError>,
    /// Готовый файл; есть у задачи в статусе done.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file: Option<DocumentJobFile>,
    /// Срок готовности; есть, пока задача в очереди или в работе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ready_within: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobAccepted {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobAccepted");
        d.field("created_at", &self.created_at);
        d.field("format", &self.format);
        d.field("job_id", &self.job_id);
        d.field("kind", &self.kind);
        d.field("lang", &self.lang);
        d.field("period", &self.period);
        d.field("status", &self.status);
        d.field("updated_at", &self.updated_at);
        d.field("error", &self.error);
        d.field("file", &self.file);
        d.field("ready_within", &self.ready_within);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobError {
    /// Машинный код отказа.
    pub code: String,
    /// Что случилось и что делать.
    pub message: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobError");
        d.field("code", &self.code);
        d.field("message", &self.message);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobFile {
    /// Путь скачивания (GET под ключом мерчанта).
    pub download_url: String,
    /// Строк в отчёте.
    pub rows: i64,
    /// Размер файла в байтах.
    pub size_bytes: i64,
    /// До какого момента файл хранится (UTC).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobFile {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobFile");
        d.field("download_url", &self.download_url);
        d.field("rows", &self.rows);
        d.field("size_bytes", &self.size_bytes);
        d.field("expires_at", &self.expires_at);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobInfoRequest {
    /// Идентификатор задачи из ответа создания.
    pub job_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobInfoRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobInfoRequest");
        d.field("job_id", &self.job_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl DocumentJobInfoRequest {
    /// A `DocumentJobInfoRequest` with the required fields; the optional ones start as `None`.
    pub fn new(job_id: impl Into<String>) -> Self {
        Self {
            job_id: job_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobPeriod {
    /// Начало периода, YYYY-MM-DD.
    pub from: String,
    /// Конец периода включительно, YYYY-MM-DD.
    pub to: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobPeriod {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobPeriod");
        d.field("from", &self.from);
        d.field("to", &self.to);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobRequest {
    /// Вид отчёта: statement (операции), fees (комиссии) или ledger (движения баланса).
    pub kind: DocumentJobKind,
    /// Формат файла: pdf (по умолчанию) или csv. CSV собирается без вёрстки — для тяжёлых выписок
    /// дешевле и грузится в Excel/1С.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub format: Option<String>,
    /// Начало периода, YYYY-MM-DD (по умолчанию — первое число текущего месяца).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from: Option<String>,
    /// Язык документа (по умолчанию en).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lang: Option<String>,
    /// Конец периода включительно, YYYY-MM-DD (по умолчанию — сегодня). Период — до двух лет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub to: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobRequest");
        d.field("kind", &self.kind);
        d.field("format", &self.format);
        d.field("from", &self.from);
        d.field("lang", &self.lang);
        d.field("to", &self.to);
        d.extra(&self.extra);
        d.finish()
    }
}

impl DocumentJobRequest {
    /// A `DocumentJobRequest` with the required fields; the optional ones start as `None`.
    pub fn new(kind: impl Into<DocumentJobKind>) -> Self {
        Self {
            kind: kind.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct DocumentJobView {
    /// Когда задача поставлена (UTC).
    pub created_at: String,
    /// Формат файла: pdf или csv.
    pub format: String,
    /// Идентификатор задачи.
    pub job_id: String,
    /// Вид отчёта.
    pub kind: DocumentJobKind,
    /// Язык документа.
    pub lang: String,
    /// Период отчёта.
    pub period: DocumentJobPeriod,
    /// Статус задачи: queued, processing, done, failed или expired.
    pub status: DocumentJobStatus,
    /// Когда задача менялась последний раз (UTC).
    pub updated_at: String,
    /// Почему файла нет; есть у задачи в статусе failed или expired.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<DocumentJobError>,
    /// Готовый файл; есть у задачи в статусе done.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub file: Option<DocumentJobFile>,
    /// Срок готовности; есть, пока задача в очереди или в работе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub ready_within: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for DocumentJobView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "DocumentJobView");
        d.field("created_at", &self.created_at);
        d.field("format", &self.format);
        d.field("job_id", &self.job_id);
        d.field("kind", &self.kind);
        d.field("lang", &self.lang);
        d.field("period", &self.period);
        d.field("status", &self.status);
        d.field("updated_at", &self.updated_at);
        d.field("error", &self.error);
        d.field("file", &self.file);
        d.field("ready_within", &self.ready_within);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct Error {
    pub error: ErrorError,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for Error {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "Error");
        d.field("error", &self.error);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ErrorError {
    /// Стабильный машинный код `<область>.<причина>` — единственное поле, по которому можно
    /// ветвиться. Список известных кодов — ErrorCode; новые коды добавляются без смены версии,
    /// поэтому клиент обязан переживать незнакомый код.
    pub code: String,
    /// true — повтор того же запроса без изменений может пройти, когда условие снимется; false —
    /// повторять бессмысленно без правки запроса.
    pub retryable: bool,
    /// Имя поля запроса, к которому относится ошибка, в присланном написании. Отсутствует, если
    /// ошибка не про конкретное поле.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub field: Option<String>,
    /// Человекочитаемое пояснение. Текст не является контрактом и может меняться.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// Идентификатор запроса (дублирует X-Request-ID) — приложите его к обращению в поддержку.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub request_id: Option<String>,
    /// Подсказка, через сколько секунд повторять (дублирует заголовок Retry-After).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub retry_after: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ErrorError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ErrorError");
        d.field("code", &self.code);
        d.field("retryable", &self.retryable);
        d.field("field", &self.field);
        d.field("message", &self.message);
        d.field("request_id", &self.request_id);
        d.field("retry_after", &self.retry_after);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExchangeRate {
    /// Цена одной единицы from в to, десятичной строкой.
    pub course: String,
    /// Исходная валюта.
    pub from: String,
    /// Валюта котировки.
    pub to: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ExchangeRate {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ExchangeRate");
        d.field("course", &self.course);
        d.field("from", &self.from);
        d.field("to", &self.to);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExchangeRatesRequest {
    /// Сумма в currency_from. Вместе с currency_from и currency_to добавляет в ответ блок modes:
    /// обе цены конвертации (instant/economy) с доступностью каждого режима
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount: Option<Money>,
    /// Код валюты. Если задан — вернётся курс только по нему. Если пусто или тело {} — по всем
    /// валютам
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency_from: Option<String>,
    /// Валюта котировки: по умолчанию USDT; любой прайсинговый актив, включая фиаты с прямым фидом
    /// (EUR, RUB, …)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency_to: Option<String>,
    /// Размер страницы, 1–100; по умолчанию 25
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Смещение от начала списка; по умолчанию 0
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub offset: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ExchangeRatesRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ExchangeRatesRequest");
        d.field("amount", &self.amount);
        d.field("currency_from", &self.currency_from);
        d.field("currency_to", &self.currency_to);
        d.field("limit", &self.limit);
        d.field("offset", &self.offset);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ExchangeRatesResult {
    /// Курсы этой страницы.
    pub items: Vec<ExchangeRate>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Квота конвертации в обоих режимах; нет ключа — квоту не просили, она не удалась или пара вне
    /// режимов.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub modes: Option<ConversionModes>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ExchangeRatesResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ExchangeRatesResult");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.field("modes", &self.modes);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FaucetRequest {
    /// Сумма тестовых денег, строкой; потолок 1000000 за вызов.
    pub amount: Money,
    /// Актив пополнения (USDT, BTC, …).
    pub asset: String,
    /// Ключ безопасного повтора; пусто — каждый вызов даёт новое пополнение.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub idempotency_key: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for FaucetRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "FaucetRequest");
        d.field("amount", &self.amount);
        d.field("asset", &self.asset);
        d.field("idempotency_key", &self.idempotency_key);
        d.extra(&self.extra);
        d.finish()
    }
}

impl FaucetRequest {
    /// A `FaucetRequest` with the required fields; the optional ones start as `None`.
    pub fn new(amount: impl Into<Money>, asset: impl Into<String>) -> Self {
        Self {
            amount: amount.into(),
            asset: asset.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct FaucetResult {
    /// Зачисленная сумма в точности актива.
    pub amount: Money,
    /// Актив пополнения.
    pub asset: String,
    /// Журнальная запись пополнения; повтор с тем же idempotency_key возвращает ту же.
    pub journal_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for FaucetResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "FaucetResult");
        d.field("amount", &self.amount);
        d.field("asset", &self.asset);
        d.field("journal_id", &self.journal_id);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct HistoryRequest {
    /// Только для /v1/payout/history: true — вместе с выплатами вернуть и возвраты (прежнее
    /// поведение ленты без kind). По умолчанию false: возвраты — отдельно, kind=refund.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub include_refunds: Option<bool>,
    /// Только для /v1/payout/history: payout — обычные выплаты, refund — возвраты; пусто — обычные
    /// выплаты (с include_refunds=true — всё вместе).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub kind: Option<PayoutKind>,
    /// Размер страницы, 1–100; вне диапазона — 25.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Смещение от начала списка (новые сверху).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub offset: Option<i64>,
    /// Фильтр по статусу (точное значение из словаря статусов); пусто — все.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for HistoryRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "HistoryRequest");
        d.field("include_refunds", &self.include_refunds);
        d.field("kind", &self.kind);
        d.field("limit", &self.limit);
        d.field("offset", &self.offset);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct LinkCheckoutRequest {
    /// Сумма, которую ввёл покупатель, в валюте цены ссылки; обязательна для open и range, для
    /// fixed игнорируется
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount: Option<Money>,
    /// Валюта расчёта — монета, которой платит покупатель; нужна, только если ссылка не закрепила
    /// pinned_currency
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency: Option<String>,
    /// Сеть расчёта; нужна, только если ссылка не закрепила pinned_network
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Номер заказа магазина из встроенного виджета (data-oblodai-order-id); переносится на счёт и
    /// в вебхук для сопоставления с заказом; не ключ идемпотентности
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Email покупателя — на него автоматически уйдёт чек после оплаты
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_email: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for LinkCheckoutRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "LinkCheckoutRequest");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("payer_email", &self.payer_email);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct LookupRequest {
    /// Ваша ссылка на заказ.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Идентификатор счёта в Oblodai. Нужен uuid или order_id; приоритет у uuid.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for LookupRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "LookupRequest");
        d.field("order_id", &self.order_id);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MassPayoutRequest {
    /// Массив до 100 элементов; поля каждого — как в POST /v1/payout.
    pub payouts: Vec<PayoutRequest>,
    /// Метка происхождения, применяется ко всем элементам без своего source.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for MassPayoutRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "MassPayoutRequest");
        d.field("payouts", &self.payouts);
        d.field("source", &self.source);
        d.extra(&self.extra);
        d.finish()
    }
}

impl MassPayoutRequest {
    /// A `MassPayoutRequest` with the required fields; the optional ones start as `None`.
    pub fn new(payouts: Vec<PayoutRequest>) -> Self {
        Self {
            payouts,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MassPayoutResult {
    /// Элементы в порядке запроса.
    pub items: Vec<MassPayoutResultItemsItem>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for MassPayoutResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "MassPayoutResult");
        d.field("items", &self.items);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MassPayoutResultItemsItem {
    /// Номер элемента в запросе.
    pub idx: i64,
    /// Элемент выполнен.
    pub ok: bool,
    /// Машинный код отказа; есть при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_code: Option<String>,
    /// HTTP-статус, которым ответил бы одиночный вызов; есть при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub http_status: Option<i64>,
    /// Текст отказа; есть при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// order_id элемента, если он был в запросе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Результат одиночного вызова; есть при ok=true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<PayoutItem>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for MassPayoutResultItemsItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "MassPayoutResultItemsItem");
        d.field("idx", &self.idx);
        d.field("ok", &self.ok);
        d.field("error_code", &self.error_code);
        d.field("http_status", &self.http_status);
        d.field("message", &self.message);
        d.field("order_id", &self.order_id);
        d.field("result", &self.result);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MerchantBalanceEntry {
    /// Доступно к выводу, десятичной строкой.
    pub balance: String,
    /// Символ актива.
    pub currency: String,
    /// Сколько этой монеты сейчас едет через очередь автоконверта (economy); нет ключа — очереди
    /// нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub converting: Option<Money>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for MerchantBalanceEntry {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "MerchantBalanceEntry");
        d.field("balance", &self.balance);
        d.field("currency", &self.currency);
        d.field("converting", &self.converting);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct MerchantBalances {
    /// Доступные балансы по активам.
    pub merchant: Vec<MerchantBalanceEntry>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for MerchantBalances {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "MerchantBalances");
        d.field("merchant", &self.merchant);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OnboardKey {
    /// Публичная часть ключа.
    pub public_id: String,
    /// Секрет ключа; пусто у повторного ответа песочницы (секрет хэширован).
    pub secret: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for OnboardKey {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "OnboardKey");
        d.field("public_id", &self.public_id);
        d.field("secret", &self.secret);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OnrampIdle {
    /// Пустая строка: живой он-рамп-сессии по счёту нет.
    pub status: OnrampIdleStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for OnrampIdle {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "OnrampIdle");
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OnrampSessionView {
    /// Срок жизни сессии (UTC).
    pub expires_at: String,
    /// Причина отказа провайдера дословно; пусто, если её нет.
    pub reason: String,
    /// Идентификатор он-рамп-сессии.
    pub session_id: String,
    /// Состояние сессии.
    pub status: OnrampStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for OnrampSessionView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "OnrampSessionView");
        d.field("expires_at", &self.expires_at);
        d.field("reason", &self.reason);
        d.field("session_id", &self.session_id);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct OnrampStartResponse {
    /// Срок жизни сессии, RFC3339 (UTC).
    pub expires_at: String,
    /// Сколько спишется с карты, в целых единицах фиата; пусто, если провайдер суммы не назвал.
    /// Оценка: курс и комиссия провайдера двигаются.
    pub fiat_amount: String,
    /// Валюта списания.
    pub fiat_currency: String,
    /// Идентификатор он-рамп-сессии.
    pub session_id: String,
    /// Состояние сессии.
    pub status: OnrampStatus,
    /// Подписанная ссылка на виджет покупки. Пустая, если покупка уже идёт: тогда смотрите status.
    pub url: String,
    /// Какой рамп дал лучшую котировку на момент открытия.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub provider: Option<String>,
    /// Причина отказа провайдера, дословно, когда она есть.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reason: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for OnrampStartResponse {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "OnrampStartResponse");
        d.field("expires_at", &self.expires_at);
        d.field("fiat_amount", &self.fiat_amount);
        d.field("fiat_currency", &self.fiat_currency);
        d.field("session_id", &self.session_id);
        d.field("status", &self.status);
        d.field("url", &self.url);
        d.field("provider", &self.provider);
        d.field("reason", &self.reason);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PageRequest {
    /// Размер страницы, 1–100; вне диапазона — 25.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Смещение от начала списка.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub offset: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PageRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PageRequest");
        d.field("limit", &self.limit);
        d.field("offset", &self.offset);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct Pagination {
    /// Есть ли записи дальше этой страницы.
    pub has_pages: bool,
    /// Смещение этой страницы.
    pub offset: i64,
    /// Размер страницы, которую отдали.
    pub per_page: i64,
    /// Всего записей по фильтру (на всех страницах).
    pub total: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for Pagination {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "Pagination");
        d.field("has_pages", &self.has_pages);
        d.field("offset", &self.offset);
        d.field("per_page", &self.per_page);
        d.field("total", &self.total);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaySelectRequest {
    /// Выбранная валюта оплаты.
    pub currency: String,
    /// Выбранная сеть.
    pub network: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaySelectRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaySelectRequest");
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PaySelectRequest {
    /// A `PaySelectRequest` with the required fields; the optional ones start as `None`.
    pub fn new(currency: impl Into<String>, network: impl Into<String>) -> Self {
        Self {
            currency: currency.into(),
            network: network.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayServiceCommission {
    /// exact — договорная ставка; estimated — оценка по сетевой комиссии.
    pub fee_type: FeeType,
    /// Единица fee_amount: USD у приёма, валюта выплаты у выплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency: Option<String>,
    /// Фиксированная часть комиссии в валюте currency; null — не определилась.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_amount: Option<Money>,
    /// Процент комиссии; null — не определился.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub percent: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayServiceCommission {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayServiceCommission");
        d.field("fee_type", &self.fee_type);
        d.field("currency", &self.currency);
        d.field("fee_amount", &self.fee_amount);
        d.field("percent", &self.percent);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayServiceEntry {
    pub commission: PayServiceCommission,
    /// Валюта.
    pub currency: String,
    /// Метод работает на этом развёртывании.
    pub is_available: bool,
    pub limit: PayServiceLimit,
    /// Сеть.
    pub network: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayServiceEntry {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayServiceEntry");
        d.field("commission", &self.commission);
        d.field("currency", &self.currency);
        d.field("is_available", &self.is_available);
        d.field("limit", &self.limit);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayServiceEntryList {
    /// Записи этой страницы.
    pub items: Vec<PayServiceEntry>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayServiceEntryList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayServiceEntryList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayServiceLimit {
    /// Потолок одной выплаты в USD; "" — потолка нет (у приёма — всегда).
    pub max_amount: String,
    /// Единица сумм limit; нет ключа — нет и границ в деньгах.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency: Option<String>,
    /// Минимальная сумма в валюте currency: "" — минимума нет, null — не определилась.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayServiceLimit {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayServiceLimit");
        d.field("max_amount", &self.max_amount);
        d.field("currency", &self.currency);
        d.field("min_amount", &self.min_amount);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentBatchItem {
    /// Сумма к оплате в валюте currency.
    pub amount: Money,
    /// Код валюты цены: любой из 23 фиатов (USD, EUR, RUB, …) или любая монета (USDT, BTC, …). У
    /// JPY и KRW ноль знаков после запятой.
    pub currency: String,
    /// Ссылка мерчанта; ключ идемпотентности. Настоятельно рекомендуется.
    pub order_id: String,
    /// Допуск недо/переплаты, 0–5 %. Перекрывает настройку мерчанта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accuracy_payment_percent: Option<f64>,
    /// Приватные данные мерчанта, эхом в вебхуках (покупателю не видны).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub additional_data: Option<String>,
    /// Разрешить доплату остатка.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_payment_multiple: Option<bool>,
    /// Оживить просроченный счёт по order_id вместо создания нового.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_refresh: Option<bool>,
    /// Время жизни счёта в секундах, 300–43200; по умолчанию 3600. Значения вне диапазона
    /// обрезаются к ближайшей границе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lifetime_seconds: Option<i64>,
    /// Сеть расчёта (напр. tron, ethereum). Необязательна — см. режимы выбора валюты и сети.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Email плательщика. Если задан — после оплаты на него автоматически уходит чек; он же
    /// получатель по умолчанию у POST /v1/payment/send-email.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_email: Option<String>,
    /// Устаревшее: % сетевой наценки на плательщика (0–100); payer-facing наценки настраиваются
    /// через discount.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subtract: Option<i64>,
    /// Тема страницы оплаты: dark | light.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub theme: Option<String>,
    /// Валюта расчёта — крипта, которой платят. По умолчанию = currency (только если currency —
    /// крипта); при цене в фиате задайте явно либо опустите вместе с network.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub to_currency: Option<String>,
    /// Индивидуальный webhook для этого счёта. Требует зарегистрированного эндпоинта (POST
    /// /v1/webhooks): доставка подписывается его секретом.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_callback: Option<String>,
    /// Ссылка «назад в магазин» на странице оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_return: Option<String>,
    /// Редирект после успешной оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_success: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentBatchItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentBatchItem");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("order_id", &self.order_id);
        d.field("accuracy_payment_percent", &self.accuracy_payment_percent);
        d.field("additional_data", &self.additional_data);
        d.field("is_payment_multiple", &self.is_payment_multiple);
        d.field("is_refresh", &self.is_refresh);
        d.field("lifetime_seconds", &self.lifetime_seconds);
        d.field("network", &self.network);
        d.field("payer_email", &self.payer_email);
        d.field("subtract", &self.subtract);
        d.field("theme", &self.theme);
        d.field("to_currency", &self.to_currency);
        d.field("url_callback", &self.url_callback);
        d.field("url_return", &self.url_return);
        d.field("url_success", &self.url_success);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentBatchRequest {
    /// Массив от 1 до 5000 элементов — те же поля, что у POST /v1/payment; order_id обязателен у
    /// каждого элемента: по нему сопоставляются результаты и он защищает от дублей.
    pub payments: Vec<PaymentBatchItem>,
    /// Что делать при ошибке элемента: continue (по умолчанию) — обрабатывать остальные; stop —
    /// прекратить обработку после первой ошибки.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub on_error: Option<BatchOnError>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentBatchRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentBatchRequest");
        d.field("payments", &self.payments);
        d.field("on_error", &self.on_error);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PaymentBatchRequest {
    /// A `PaymentBatchRequest` with the required fields; the optional ones start as `None`.
    pub fn new(payments: Vec<PaymentBatchItem>) -> Self {
        Self {
            payments,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentDiscountRule {
    /// Монета правила. Пусто — правило по умолчанию для всех монет, у которых нет своего.
    pub currency: String,
    /// Процент, от -99 до 99. Плюс — скидка плательщику за оплату этой монетой, минус — наценка.
    pub discount_percent: i64,
    /// Сеть. Пусто — любая сеть этой монеты.
    pub network: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentDiscountRule {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentDiscountRule");
        d.field("currency", &self.currency);
        d.field("discount_percent", &self.discount_percent);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentDiscountRuleList {
    /// Записи этой страницы.
    pub items: Vec<PaymentDiscountRule>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentDiscountRuleList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentDiscountRuleList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentFeeResult {
    /// Разрешён ли мерчанту перенос комиссии на покупателя (решение оператора).
    pub enabled: bool,
    /// Доля, которую применит следующий счёт; 0, если оператор выключил перенос комиссии.
    pub payer_pays_percent: i64,
    /// Фиксированная часть комиссии на платёж, USD десятичной строкой.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_fixed_usd: Option<Money>,
    /// Устарело: та же фиксированная часть целыми центами США числом — читайте fee_fixed_usd.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_fixed_usd_cents: Option<i64>,
    /// true — персональный тариф; false — умолчание платформы.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_individual: Option<bool>,
    /// Процент комиссии мерчанта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_percent: Option<Money>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentFeeResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentFeeResult");
        d.field("enabled", &self.enabled);
        d.field("payer_pays_percent", &self.payer_pays_percent);
        d.field("fee_fixed_usd", &self.fee_fixed_usd);
        d.field("fee_fixed_usd_cents", &self.fee_fixed_usd_cents);
        d.field("fee_individual", &self.fee_individual);
        d.field("fee_percent", &self.fee_percent);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentInfoResult {
    /// Ваши приватные данные, которые вернутся в ответе и в вебхуке.
    pub additional_data: String,
    /// Адрес, на который клиент отправляет деньги. На XRP это классический r-адрес ОБЩЕГО кошелька
    /// — платёж обязан нести destination_tag, иначе сеть его отклонит.
    pub address: String,
    /// Только XLM: те же реквизиты одной строкой — muxed-адрес M… (SEP-23), адрес и memo вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_muxed: String,
    /// QR-код адреса как PNG data:-URI — можно сразу в \<img src\>. На XRP кодирует X-address
    /// (адрес+тег одной строкой).
    pub address_qr_code: String,
    /// Только XRP: те же реквизиты одной строкой в формате X-address (XLS-5) — адрес и тег вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_xaddress: String,
    /// Сумма к оплате в валюте цены (например, в USD).
    pub amount: Money,
    /// Сколько уже подтверждённо оплачено, в крипте оплаты; всегда строка (0, если ничего не
    /// пришло). Пусто, пока валюта оплаты не выбрана (счёт без валюты).
    pub amount_paid: String,
    /// Сколько ещё осталось доплатить (к оплате − оплачено); 0, если хватает. Пусто, пока валюта
    /// оплаты не выбрана (счёт без валюты).
    pub amount_remaining: String,
    /// Наша комиссия с этого платежа — УДЕРЖАННАЯ величина, в валюте оплаты (payer_currency).
    /// Ставка счёта уже включает амортизированный фиксированный сбор — второй раз он не берётся.
    /// ПУСТО, пока по счёту ничего не зачислено (и у валюто-агностичного счёта до выбора монеты):
    /// нуля здесь не бывает у неоплаченного счёта — «0» читалось бы как «комиссию не берут». У
    /// оплаченного счёта с нулевым тарифом 0 — настоящий.
    pub commission: String,
    /// Текущее число подтверждений входящего платежа.
    pub confirmations: i64,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Валюта цены: фиат (USD, EUR, RUB, JPY… — см. pricing_currencies) или монета. Говорит,
    /// сколько счёт СТОИТ, а не чем за него платят (это payer_currency).
    pub currency: String,
    /// Только XRP: числовой destination tag, который клиент ОБЯЗАН указать в переводе (поле
    /// «тег/memo получателя» на бирже или в кошельке). Пусто на остальных сетях.
    pub destination_tag: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать клиенту. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Курс, зафиксированный этим счётом (сколько валюты оплаты за 1 единицу валюты цены) — по нему
    /// рассчитан payer_amount. Пусто, пока валюта не выбрана.
    pub exchange_rate: String,
    /// Когда истекает счёт (ISO 8601, как и все временные поля).
    pub expired_at: String,
    /// Ставка комиссии этого счёта в процентах — та, что зафиксирована в момент создания (смена
    /// тарифа не меняет уже созданные счета). Уже включает амортизированный фиксированный сбор. В
    /// отличие от commission известна с первой секунды и присутствует всегда.
    pub fee_percent: Money,
    /// true — статус финальный, больше не изменится.
    pub is_final: bool,
    /// true — это валюто-агностичная ссылка, клиент ещё не выбрал валюту/сеть.
    pub is_multi: bool,
    /// true — счёт песочницы (dev-магазина): деньги ненастоящие, в живую сверку не включайте.
    pub is_test: bool,
    /// Только XLM (Stellar): числовой memo (тип ID), который клиент ОБЯЗАН указать в переводе —
    /// поле «memo» на бирже или в кошельке. Пусто на остальных сетях.
    pub memo: String,
    /// Сколько зачислено (или будет зачислено) вам: amount_paid − network_surcharge − commission.
    /// Сетевые расходы на сбор депозита оплачивает плательщик отдельной строкой (network_surcharge)
    /// — из вашей суммы они НЕ вычитаются. Пусто, пока валюта оплаты не выбрана (счёт без валюты).
    pub merchant_amount: String,
    /// Ваша скидка или наценка для ВЫБРАННОГО способа оплаты, в валюте оплаты: на столько
    /// сдвинулась сумма плательщика из-за настройки по этой монете и сети. Положительное —
    /// плательщик платит МЕНЬШЕ (скидка), отрицательное — больше (наценка). Пусто, если настройки
    /// для метода нет.
    pub method_adjustment: String,
    /// Та же скидка/наценка в базисных пунктах (так она переживает переоценку курса). Знак тот же,
    /// что в настройке скидок: ПЛЮС — скидка, МИНУС — наценка.
    pub method_adjustment_bps: i64,
    /// Сеть блокчейна (например, tron).
    pub network: String,
    /// Сетевая надбавка плательщика в валюте оплаты: стоимость сбора депозита в выбранной сети
    /// (активация адреса, если адрес новый, плюс энергия/газ с запасом), зафиксированная при выборе
    /// сети. Пусто до выбора сети; 0, если надбавка выключена.
    pub network_surcharge: String,
    /// Та же надбавка в базисных пунктах от суммы к оплате (так она переживает переоценку курса).
    pub network_surcharge_bps: i64,
    /// Ваш номер заказа, который вы передали при создании.
    pub order_id: String,
    /// Адрес, С КОТОРОГО пришёл первый подтверждённый депозит — на аккаунт-сетях
    /// (EVM/Tron/Solana/TON); пусто на UTXO. ⚠ Это НЕ обязательно адрес для возврата: отправителем
    /// может быть биржа, сдача UTXO-транзакции или горячий омнибус крипто-он-рампа, если покупатель
    /// платил картой. Прежде чем возвращать деньги сюда, смотрите payer_address_is_refundable.
    pub payer_address: String,
    /// true — payer_address принадлежит плательщику, и в /v1/payment/refund можно опустить address
    /// (вернём на него). false — адрес возврата неизвестен (UTXO/XRP, оплата картой через он-рамп,
    /// адрес не записан): спросите адрес у покупателя и передайте address явно, иначе запрос будет
    /// отклонён с refund.no_address.
    pub payer_address_is_refundable: bool,
    /// Сколько нужно отправить в крипте оплаты. Пусто, пока валюта оплаты не выбрана (счёт без
    /// валюты).
    pub payer_amount: String,
    /// Валюта, в которой платит клиент (например, USDT). Пусто у валюто-агностичного счёта
    /// (is_multi), пока клиент не выбрал монету — валюты расчёта у него ещё нет.
    pub payer_currency: String,
    /// E-mail плательщика, если вы его передали.
    pub payer_email: String,
    /// До какого момента действует зафиксированный payer_amount (ISO 8601; окно ~5 мин, после него
    /// страница оплаты перекотирует счёт). Пусто, когда перекотировки уже не будет: валюта не
    /// выбрана, депозит замечен, счёт вышел из created или истёк — сумма зафиксирована навсегда.
    pub rate_expires_at: String,
    /// Сколько подтверждений нужно для зачисления (зависит от суммы и сети).
    pub required_confirmations: i64,
    /// Статус: select (клиент выбирает валюту) | created (ждём оплату) | confirm_check (видим
    /// оплату, ждём подтверждений; при amount_remaining \> 0 — частичная, ждём остаток) | paid
    /// (оплачено) | paid_over (переплата) | wrong_amount (недоплата, срок вышел) | expired
    /// (просрочен) | cancelled (отменён) | under_review (поступление задержано на проверке,
    /// разбирает оператор).
    pub status: PaymentStatus,
    /// Все подтверждённые переводы, которыми оплачен счёт. Частичная оплата несколькими переводами
    /// — штатный сценарий wrong_amount; один txid наверху — лишь последний замеченный.
    pub tx_list: Vec<PaymentTx>,
    /// Хеш входящей транзакции (когда замечена).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Ссылка на готовую страницу оплаты.
    pub url: String,
    /// Ссылка «вернуться в магазин» до оплаты.
    pub url_return: String,
    /// Куда перенаправить после успешной оплаты.
    pub url_success: String,
    /// Наш идентификатор платежа (используйте его в info/refund).
    pub uuid: String,
    /// Момент фактической оплаты — зачисление последнего подтверждённого перевода (ISO 8601). null,
    /// пока оплата не пришла. Отличайте от updated_at: тот сдвигается любым изменением счёта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub paid_at: Option<String>,
    /// Сколько возвращено от оплаченного: none, partial или full (отменённые и неудавшиеся возвраты
    /// не считаются).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_status: Option<RefundRollup>,
    /// Возвраты по этому платежу.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refunds: Option<Vec<PaymentRefundLine>>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentInfoResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentInfoResult");
        d.field("additional_data", &self.additional_data);
        d.field("address", &self.address);
        d.field("address_muxed", &self.address_muxed);
        d.field("address_qr_code", &self.address_qr_code);
        d.field("address_xaddress", &self.address_xaddress);
        d.field("amount", &self.amount);
        d.field("amount_paid", &self.amount_paid);
        d.field("amount_remaining", &self.amount_remaining);
        d.field("commission", &self.commission);
        d.field("confirmations", &self.confirmations);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("destination_tag", &self.destination_tag);
        d.field("document_url", &self.document_url);
        d.field("exchange_rate", &self.exchange_rate);
        d.field("expired_at", &self.expired_at);
        d.field("fee_percent", &self.fee_percent);
        d.field("is_final", &self.is_final);
        d.field("is_multi", &self.is_multi);
        d.field("is_test", &self.is_test);
        d.field("memo", &self.memo);
        d.field("merchant_amount", &self.merchant_amount);
        d.field("method_adjustment", &self.method_adjustment);
        d.field("method_adjustment_bps", &self.method_adjustment_bps);
        d.field("network", &self.network);
        d.field("network_surcharge", &self.network_surcharge);
        d.field("network_surcharge_bps", &self.network_surcharge_bps);
        d.field("order_id", &self.order_id);
        d.field("payer_address", &self.payer_address);
        d.field(
            "payer_address_is_refundable",
            &self.payer_address_is_refundable,
        );
        d.field("payer_amount", &self.payer_amount);
        d.field("payer_currency", &self.payer_currency);
        d.field("payer_email", &self.payer_email);
        d.field("rate_expires_at", &self.rate_expires_at);
        d.field("required_confirmations", &self.required_confirmations);
        d.field("status", &self.status);
        d.field("tx_list", &self.tx_list);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("url", &self.url);
        d.field("url_return", &self.url_return);
        d.field("url_success", &self.url_success);
        d.field("uuid", &self.uuid);
        d.field("paid_at", &self.paid_at);
        d.field("refund_status", &self.refund_status);
        d.field("refunds", &self.refunds);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkCreateRequest {
    /// Режим суммы: fixed | open | range
    pub amount_mode: AmountMode,
    /// Валюта цены — фиат (USD, EUR, RUB, …) или монета; список — pricing_currencies из GET
    /// /v1/currencies
    pub currency: String,
    /// Сумма — для режима fixed; обязательна в этом режиме
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount_fixed: Option<String>,
    /// Описание на странице оплаты
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub description: Option<String>,
    /// Срок жизни ссылки, секунд от момента создания; 0 (по умолчанию) — ссылка бессрочная
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_in_seconds: Option<i64>,
    /// Верхняя граница — для range; обязательна в этом режиме
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_amount: Option<Money>,
    /// Нижняя граница: необязательный «пол» для open, обязательный минимум для range
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<Money>,
    /// Валюта расчёта (монета), закреплённая за ссылкой; пусто — монету выбирает покупатель
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_currency: Option<String>,
    /// Сеть расчёта, закреплённая за ссылкой; пусто — сеть выбирает покупатель
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_network: Option<String>,
    /// Заголовок на странице оплаты
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkCreateRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkCreateRequest");
        d.field("amount_mode", &self.amount_mode);
        d.field("currency", &self.currency);
        d.field("amount_fixed", &self.amount_fixed);
        d.field("description", &self.description);
        d.field("expires_in_seconds", &self.expires_in_seconds);
        d.field("max_amount", &self.max_amount);
        d.field("min_amount", &self.min_amount);
        d.field("pinned_currency", &self.pinned_currency);
        d.field("pinned_network", &self.pinned_network);
        d.field("title", &self.title);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PaymentLinkCreateRequest {
    /// A `PaymentLinkCreateRequest` with the required fields; the optional ones start as `None`.
    pub fn new(amount_mode: impl Into<AmountMode>, currency: impl Into<String>) -> Self {
        Self {
            amount_mode: amount_mode.into(),
            currency: currency.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkDetail {
    /// Ссылка принимает оплату.
    pub active: bool,
    /// Режим суммы.
    pub amount_mode: AmountMode,
    /// Когда создана (UTC).
    pub created_at: String,
    /// Валюта цены.
    pub currency: String,
    /// Описание на странице оплаты.
    pub description: String,
    /// Подписанная ссылка на PDF-плакат с QR оплаты; пусто, когда рендер документов не включён.
    pub document_url: String,
    /// Идентификатор ссылки.
    pub link_id: String,
    /// Платежи по ссылке, страница по limit/offset запроса.
    pub payments: Vec<PaymentLinkPayment>,
    /// Заголовок страницы оплаты.
    pub title: String,
    /// Публичный URL страницы оплаты; пусто, если публичный адрес не настроен.
    pub url: String,
    /// Сумма для режима fixed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount_fixed: Option<Money>,
    /// Когда ссылка истекает (UTC); нет — бессрочная.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<String>,
    /// Верхняя граница для range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_amount: Option<Money>,
    /// Нижняя граница для open/range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<Money>,
    /// Закреплённая валюта оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_currency: Option<String>,
    /// Закреплённая сеть оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_network: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkDetail {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkDetail");
        d.field("active", &self.active);
        d.field("amount_mode", &self.amount_mode);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("description", &self.description);
        d.field("document_url", &self.document_url);
        d.field("link_id", &self.link_id);
        d.field("payments", &self.payments);
        d.field("title", &self.title);
        d.field("url", &self.url);
        d.field("amount_fixed", &self.amount_fixed);
        d.field("expires_at", &self.expires_at);
        d.field("max_amount", &self.max_amount);
        d.field("min_amount", &self.min_amount);
        d.field("pinned_currency", &self.pinned_currency);
        d.field("pinned_network", &self.pinned_network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkLookupRequest {
    /// Идентификатор платёжной ссылки.
    pub link_id: String,
    /// Размер страницы платежей по ссылке, 1–100; вне диапазона — 25.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub limit: Option<i64>,
    /// Смещение страницы платежей.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub offset: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkLookupRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkLookupRequest");
        d.field("link_id", &self.link_id);
        d.field("limit", &self.limit);
        d.field("offset", &self.offset);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PaymentLinkLookupRequest {
    /// A `PaymentLinkLookupRequest` with the required fields; the optional ones start as `None`.
    pub fn new(link_id: impl Into<String>) -> Self {
        Self {
            link_id: link_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkPayment {
    /// Цена счёта в валюте цены ссылки.
    pub amount: Money,
    /// Когда создан (UTC).
    pub created_at: String,
    /// Валюта цены.
    pub currency: String,
    /// Статус платежа.
    pub status: PaymentStatus,
    /// Идентификатор платежа.
    pub uuid: String,
    /// Номер заказа магазина, если виджет его передал.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkPayment {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkPayment");
        d.field("amount", &self.amount);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("status", &self.status);
        d.field("uuid", &self.uuid);
        d.field("order_id", &self.order_id);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkPublicView {
    /// fixed, open или range.
    pub amount_mode: AmountMode,
    /// Валюта цены.
    pub currency: String,
    /// Описание.
    pub description: String,
    /// Идентификатор ссылки.
    pub link_id: String,
    /// Заголовок страницы.
    pub title: String,
    /// Сумма для fixed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount_fixed: Option<String>,
    /// Верхняя граница для range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_amount: Option<Money>,
    /// Нижняя граница для open/range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<Money>,
    /// Закреплённая валюта оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_currency: Option<String>,
    /// Закреплённая сеть оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_network: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkPublicView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkPublicView");
        d.field("amount_mode", &self.amount_mode);
        d.field("currency", &self.currency);
        d.field("description", &self.description);
        d.field("link_id", &self.link_id);
        d.field("title", &self.title);
        d.field("amount_fixed", &self.amount_fixed);
        d.field("max_amount", &self.max_amount);
        d.field("min_amount", &self.min_amount);
        d.field("pinned_currency", &self.pinned_currency);
        d.field("pinned_network", &self.pinned_network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkResponse {
    /// Подписанная ссылка на PDF-плакат с QR оплаты (печать на кассу). Пусто, если генерация
    /// документов не включена.
    pub document_url: String,
    /// Идентификатор ссылки
    pub link_id: String,
    /// Публичный URL страницы оплаты — его вы даёте покупателю: кнопкой, в письме, QR-кодом
    pub url: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkResponse {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkResponse");
        d.field("document_url", &self.document_url);
        d.field("link_id", &self.link_id);
        d.field("url", &self.url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkToggleRequest {
    /// true — ссылка принимает оплату; false — выключена (страница покажет, что ссылка неактивна).
    pub active: bool,
    /// Идентификатор платёжной ссылки.
    pub link_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkToggleRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkToggleRequest");
        d.field("active", &self.active);
        d.field("link_id", &self.link_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PaymentLinkToggleRequest {
    /// A `PaymentLinkToggleRequest` with the required fields; the optional ones start as `None`.
    pub fn new(active: bool, link_id: impl Into<String>) -> Self {
        Self {
            active,
            link_id: link_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkToggled {
    /// Новое состояние: true — принимает оплату.
    pub active: bool,
    /// Идентификатор ссылки.
    pub link_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkToggled {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkToggled");
        d.field("active", &self.active);
        d.field("link_id", &self.link_id);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkView {
    /// Ссылка принимает оплату.
    pub active: bool,
    /// Режим суммы.
    pub amount_mode: AmountMode,
    /// Когда создана (UTC).
    pub created_at: String,
    /// Валюта цены.
    pub currency: String,
    /// Описание на странице оплаты.
    pub description: String,
    /// Подписанная ссылка на PDF-плакат с QR оплаты; пусто, когда рендер документов не включён.
    pub document_url: String,
    /// Идентификатор ссылки.
    pub link_id: String,
    /// Заголовок страницы оплаты.
    pub title: String,
    /// Публичный URL страницы оплаты; пусто, если публичный адрес не настроен.
    pub url: String,
    /// Сумма для режима fixed.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount_fixed: Option<Money>,
    /// Когда ссылка истекает (UTC); нет — бессрочная.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_at: Option<String>,
    /// Верхняя граница для range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub max_amount: Option<Money>,
    /// Нижняя граница для open/range.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<Money>,
    /// Закреплённая валюта оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_currency: Option<String>,
    /// Закреплённая сеть оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub pinned_network: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkView");
        d.field("active", &self.active);
        d.field("amount_mode", &self.amount_mode);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("description", &self.description);
        d.field("document_url", &self.document_url);
        d.field("link_id", &self.link_id);
        d.field("title", &self.title);
        d.field("url", &self.url);
        d.field("amount_fixed", &self.amount_fixed);
        d.field("expires_at", &self.expires_at);
        d.field("max_amount", &self.max_amount);
        d.field("min_amount", &self.min_amount);
        d.field("pinned_currency", &self.pinned_currency);
        d.field("pinned_network", &self.pinned_network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentLinkViewList {
    /// Записи этой страницы.
    pub items: Vec<PaymentLinkView>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentLinkViewList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentLinkViewList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentQRResult {
    /// Депозитный адрес; пусто, пока его нет.
    pub address: String,
    /// PNG QR-кода как data:-URI; "" — адреса ещё нет (монета не выбрана) или он не платёжный
    /// (песочница).
    pub image: String,
    /// true — в QR платёжный запрос с суммой (кошелёк подставит её сам); false — только адрес,
    /// сумму плательщик вводит.
    pub is_uri: bool,
    /// Что закодировано в QR: платёжный URI сети с суммой или голый адрес.
    pub payload: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentQRResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentQRResult");
        d.field("address", &self.address);
        d.field("image", &self.image);
        d.field("is_uri", &self.is_uri);
        d.field("payload", &self.payload);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentRefundLine {
    /// Куда возвращено.
    pub address: String,
    /// Сумма возврата в монете платежа.
    pub amount: Money,
    /// Когда создан (RFC 3339).
    pub created_at: String,
    /// Статус возврата окончательный.
    pub is_final: bool,
    /// Статус выплаты-возврата.
    pub status: PayoutStatus,
    /// Хэш транзакции возврата; пусто, пока не отправлен.
    pub txid: String,
    /// Идентификатор возврата (это выплата).
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentRefundLine {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentRefundLine");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("created_at", &self.created_at);
        d.field("is_final", &self.is_final);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentRequest {
    /// Сумма к оплате в валюте currency.
    pub amount: Money,
    /// Код валюты цены: любой из 23 фиатов (USD, EUR, RUB, …) или любая монета (USDT, BTC, …). У
    /// JPY и KRW ноль знаков после запятой.
    pub currency: String,
    /// Допуск недо/переплаты, 0–5 %. Перекрывает настройку мерчанта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accuracy_payment_percent: Option<f64>,
    /// Приватные данные мерчанта, эхом в вебхуках (покупателю не видны).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub additional_data: Option<String>,
    /// Разрешить доплату остатка.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_payment_multiple: Option<bool>,
    /// Оживить просроченный счёт по order_id вместо создания нового.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_refresh: Option<bool>,
    /// Время жизни счёта в секундах, 300–43200; по умолчанию 3600. Значения вне диапазона
    /// обрезаются к ближайшей границе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lifetime_seconds: Option<i64>,
    /// Сеть расчёта (напр. tron, ethereum). Необязательна — см. режимы выбора валюты и сети.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Ссылка мерчанта; ключ идемпотентности. Настоятельно рекомендуется.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Email плательщика. Если задан — после оплаты на него автоматически уходит чек; он же
    /// получатель по умолчанию у POST /v1/payment/send-email.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_email: Option<String>,
    /// Устаревшее: % сетевой наценки на плательщика (0–100); payer-facing наценки настраиваются
    /// через discount.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub subtract: Option<i64>,
    /// Тема страницы оплаты: dark | light.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub theme: Option<String>,
    /// Валюта расчёта — крипта, которой платят. По умолчанию = currency (только если currency —
    /// крипта); при цене в фиате задайте явно либо опустите вместе с network.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub to_currency: Option<String>,
    /// Индивидуальный webhook для этого счёта. Требует зарегистрированного эндпоинта (POST
    /// /v1/webhooks): доставка подписывается его секретом.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_callback: Option<String>,
    /// Ссылка «назад в магазин» на странице оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_return: Option<String>,
    /// Редирект после успешной оплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_success: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentRequest");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("accuracy_payment_percent", &self.accuracy_payment_percent);
        d.field("additional_data", &self.additional_data);
        d.field("is_payment_multiple", &self.is_payment_multiple);
        d.field("is_refresh", &self.is_refresh);
        d.field("lifetime_seconds", &self.lifetime_seconds);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("payer_email", &self.payer_email);
        d.field("subtract", &self.subtract);
        d.field("theme", &self.theme);
        d.field("to_currency", &self.to_currency);
        d.field("url_callback", &self.url_callback);
        d.field("url_return", &self.url_return);
        d.field("url_success", &self.url_success);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PaymentRequest {
    /// A `PaymentRequest` with the required fields; the optional ones start as `None`.
    pub fn new(amount: impl Into<Money>, currency: impl Into<String>) -> Self {
        Self {
            amount: amount.into(),
            currency: currency.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentTx {
    /// Сумма перевода в валюте оплаты.
    pub amount: Money,
    /// Когда перевод зачислен (ISO 8601).
    pub created_at: String,
    /// Высота блока, в котором перевод подтверждён.
    pub height: i64,
    /// Сеть, в которой пришёл перевод. На EVM может отличаться от network счёта: депозит
    /// зачитывается и на другой цепочке с тем же адресом.
    pub network: String,
    /// Хеш транзакции.
    pub txid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentTx {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentTx");
        d.field("amount", &self.amount);
        d.field("created_at", &self.created_at);
        d.field("height", &self.height);
        d.field("network", &self.network);
        d.field("txid", &self.txid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentView {
    /// Ваши приватные данные, которые вернутся в ответе и в вебхуке.
    pub additional_data: String,
    /// Адрес, на который клиент отправляет деньги. На XRP это классический r-адрес ОБЩЕГО кошелька
    /// — платёж обязан нести destination_tag, иначе сеть его отклонит.
    pub address: String,
    /// Только XLM: те же реквизиты одной строкой — muxed-адрес M… (SEP-23), адрес и memo вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_muxed: String,
    /// QR-код адреса как PNG data:-URI — можно сразу в \<img src\>. На XRP кодирует X-address
    /// (адрес+тег одной строкой).
    pub address_qr_code: String,
    /// Только XRP: те же реквизиты одной строкой в формате X-address (XLS-5) — адрес и тег вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_xaddress: String,
    /// Сумма к оплате в валюте цены (например, в USD).
    pub amount: Money,
    /// Сколько уже подтверждённо оплачено, в крипте оплаты; всегда строка (0, если ничего не
    /// пришло). Пусто, пока валюта оплаты не выбрана (счёт без валюты).
    pub amount_paid: String,
    /// Сколько ещё осталось доплатить (к оплате − оплачено); 0, если хватает. Пусто, пока валюта
    /// оплаты не выбрана (счёт без валюты).
    pub amount_remaining: String,
    /// Наша комиссия с этого платежа — УДЕРЖАННАЯ величина, в валюте оплаты (payer_currency).
    /// Ставка счёта уже включает амортизированный фиксированный сбор — второй раз он не берётся.
    /// ПУСТО, пока по счёту ничего не зачислено (и у валюто-агностичного счёта до выбора монеты):
    /// нуля здесь не бывает у неоплаченного счёта — «0» читалось бы как «комиссию не берут». У
    /// оплаченного счёта с нулевым тарифом 0 — настоящий.
    pub commission: String,
    /// Текущее число подтверждений входящего платежа.
    pub confirmations: i64,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Валюта цены: фиат (USD, EUR, RUB, JPY… — см. pricing_currencies) или монета. Говорит,
    /// сколько счёт СТОИТ, а не чем за него платят (это payer_currency).
    pub currency: String,
    /// Только XRP: числовой destination tag, который клиент ОБЯЗАН указать в переводе (поле
    /// «тег/memo получателя» на бирже или в кошельке). Пусто на остальных сетях.
    pub destination_tag: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать клиенту. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Курс, зафиксированный этим счётом (сколько валюты оплаты за 1 единицу валюты цены) — по нему
    /// рассчитан payer_amount. Пусто, пока валюта не выбрана.
    pub exchange_rate: String,
    /// Когда истекает счёт (ISO 8601, как и все временные поля).
    pub expired_at: String,
    /// Ставка комиссии этого счёта в процентах — та, что зафиксирована в момент создания (смена
    /// тарифа не меняет уже созданные счета). Уже включает амортизированный фиксированный сбор. В
    /// отличие от commission известна с первой секунды и присутствует всегда.
    pub fee_percent: Money,
    /// true — статус финальный, больше не изменится.
    pub is_final: bool,
    /// true — это валюто-агностичная ссылка, клиент ещё не выбрал валюту/сеть.
    pub is_multi: bool,
    /// true — счёт песочницы (dev-магазина): деньги ненастоящие, в живую сверку не включайте.
    pub is_test: bool,
    /// Только XLM (Stellar): числовой memo (тип ID), который клиент ОБЯЗАН указать в переводе —
    /// поле «memo» на бирже или в кошельке. Пусто на остальных сетях.
    pub memo: String,
    /// Сколько зачислено (или будет зачислено) вам: amount_paid − network_surcharge − commission.
    /// Сетевые расходы на сбор депозита оплачивает плательщик отдельной строкой (network_surcharge)
    /// — из вашей суммы они НЕ вычитаются. Пусто, пока валюта оплаты не выбрана (счёт без валюты).
    pub merchant_amount: String,
    /// Ваша скидка или наценка для ВЫБРАННОГО способа оплаты, в валюте оплаты: на столько
    /// сдвинулась сумма плательщика из-за настройки по этой монете и сети. Положительное —
    /// плательщик платит МЕНЬШЕ (скидка), отрицательное — больше (наценка). Пусто, если настройки
    /// для метода нет.
    pub method_adjustment: String,
    /// Та же скидка/наценка в базисных пунктах (так она переживает переоценку курса). Знак тот же,
    /// что в настройке скидок: ПЛЮС — скидка, МИНУС — наценка.
    pub method_adjustment_bps: i64,
    /// Сеть блокчейна (например, tron).
    pub network: String,
    /// Сетевая надбавка плательщика в валюте оплаты: стоимость сбора депозита в выбранной сети
    /// (активация адреса, если адрес новый, плюс энергия/газ с запасом), зафиксированная при выборе
    /// сети. Пусто до выбора сети; 0, если надбавка выключена.
    pub network_surcharge: String,
    /// Та же надбавка в базисных пунктах от суммы к оплате (так она переживает переоценку курса).
    pub network_surcharge_bps: i64,
    /// Ваш номер заказа, который вы передали при создании.
    pub order_id: String,
    /// Адрес, С КОТОРОГО пришёл первый подтверждённый депозит — на аккаунт-сетях
    /// (EVM/Tron/Solana/TON); пусто на UTXO. ⚠ Это НЕ обязательно адрес для возврата: отправителем
    /// может быть биржа, сдача UTXO-транзакции или горячий омнибус крипто-он-рампа, если покупатель
    /// платил картой. Прежде чем возвращать деньги сюда, смотрите payer_address_is_refundable.
    pub payer_address: String,
    /// true — payer_address принадлежит плательщику, и в /v1/payment/refund можно опустить address
    /// (вернём на него). false — адрес возврата неизвестен (UTXO/XRP, оплата картой через он-рамп,
    /// адрес не записан): спросите адрес у покупателя и передайте address явно, иначе запрос будет
    /// отклонён с refund.no_address.
    pub payer_address_is_refundable: bool,
    /// Сколько нужно отправить в крипте оплаты. Пусто, пока валюта оплаты не выбрана (счёт без
    /// валюты).
    pub payer_amount: String,
    /// Валюта, в которой платит клиент (например, USDT). Пусто у валюто-агностичного счёта
    /// (is_multi), пока клиент не выбрал монету — валюты расчёта у него ещё нет.
    pub payer_currency: String,
    /// E-mail плательщика, если вы его передали.
    pub payer_email: String,
    /// До какого момента действует зафиксированный payer_amount (ISO 8601; окно ~5 мин, после него
    /// страница оплаты перекотирует счёт). Пусто, когда перекотировки уже не будет: валюта не
    /// выбрана, депозит замечен, счёт вышел из created или истёк — сумма зафиксирована навсегда.
    pub rate_expires_at: String,
    /// Сколько подтверждений нужно для зачисления (зависит от суммы и сети).
    pub required_confirmations: i64,
    /// Статус: select (клиент выбирает валюту) | created (ждём оплату) | confirm_check (видим
    /// оплату, ждём подтверждений; при amount_remaining \> 0 — частичная, ждём остаток) | paid
    /// (оплачено) | paid_over (переплата) | wrong_amount (недоплата, срок вышел) | expired
    /// (просрочен) | cancelled (отменён) | under_review (поступление задержано на проверке,
    /// разбирает оператор).
    pub status: PaymentStatus,
    /// Все подтверждённые переводы, которыми оплачен счёт. Частичная оплата несколькими переводами
    /// — штатный сценарий wrong_amount; один txid наверху — лишь последний замеченный.
    pub tx_list: Vec<PaymentTx>,
    /// Хеш входящей транзакции (когда замечена).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Ссылка на готовую страницу оплаты.
    pub url: String,
    /// Ссылка «вернуться в магазин» до оплаты.
    pub url_return: String,
    /// Куда перенаправить после успешной оплаты.
    pub url_success: String,
    /// Наш идентификатор платежа (используйте его в info/refund).
    pub uuid: String,
    /// Момент фактической оплаты — зачисление последнего подтверждённого перевода (ISO 8601). null,
    /// пока оплата не пришла. Отличайте от updated_at: тот сдвигается любым изменением счёта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub paid_at: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentView");
        d.field("additional_data", &self.additional_data);
        d.field("address", &self.address);
        d.field("address_muxed", &self.address_muxed);
        d.field("address_qr_code", &self.address_qr_code);
        d.field("address_xaddress", &self.address_xaddress);
        d.field("amount", &self.amount);
        d.field("amount_paid", &self.amount_paid);
        d.field("amount_remaining", &self.amount_remaining);
        d.field("commission", &self.commission);
        d.field("confirmations", &self.confirmations);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("destination_tag", &self.destination_tag);
        d.field("document_url", &self.document_url);
        d.field("exchange_rate", &self.exchange_rate);
        d.field("expired_at", &self.expired_at);
        d.field("fee_percent", &self.fee_percent);
        d.field("is_final", &self.is_final);
        d.field("is_multi", &self.is_multi);
        d.field("is_test", &self.is_test);
        d.field("memo", &self.memo);
        d.field("merchant_amount", &self.merchant_amount);
        d.field("method_adjustment", &self.method_adjustment);
        d.field("method_adjustment_bps", &self.method_adjustment_bps);
        d.field("network", &self.network);
        d.field("network_surcharge", &self.network_surcharge);
        d.field("network_surcharge_bps", &self.network_surcharge_bps);
        d.field("order_id", &self.order_id);
        d.field("payer_address", &self.payer_address);
        d.field(
            "payer_address_is_refundable",
            &self.payer_address_is_refundable,
        );
        d.field("payer_amount", &self.payer_amount);
        d.field("payer_currency", &self.payer_currency);
        d.field("payer_email", &self.payer_email);
        d.field("rate_expires_at", &self.rate_expires_at);
        d.field("required_confirmations", &self.required_confirmations);
        d.field("status", &self.status);
        d.field("tx_list", &self.tx_list);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("url", &self.url);
        d.field("url_return", &self.url_return);
        d.field("url_success", &self.url_success);
        d.field("uuid", &self.uuid);
        d.field("paid_at", &self.paid_at);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentViewList {
    /// Записи этой страницы.
    pub items: Vec<PaymentView>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentViewList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentViewList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

/// Приходит, когда платёж переходит в paid, paid_over, wrong_amount, expired или under_review, и
/// когда откатывается из них (реорганизация сети). Текущий статус — любой из словаря — можно
/// запросить заново: POST /v1/payment/resend. Сверять с заказом по order_id/uuid, с блокчейном — по
/// txid и network.
#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PaymentWebhook {
    /// Ваши данные, переданные при создании платежа, как есть.
    pub additional_data: String,
    /// Сумма счёта в валюте currency.
    pub amount: Money,
    /// Валюта счёта.
    pub currency: String,
    /// Когда событие произошло, UTC с миллисекундами (ISO 8601).
    pub event_at: String,
    /// true — статус финальный, дальше платёж не изменится.
    pub is_final: bool,
    /// Сеть, в которой пришли деньги.
    pub network: String,
    /// Ваш order_id платежа.
    pub order_id: String,
    /// Адрес, с которого пришёл платёж (пусто, если неизвестен). Возвращать на него можно только
    /// при payer_address_is_refundable = true.
    pub payer_address: String,
    /// true — payer_address принадлежит плательщику и годится как адрес возврата; false — это адрес
    /// биржи, провайдера карты или сдачи, возвращать на него нельзя.
    pub payer_address_is_refundable: bool,
    /// Сколько плательщик должен был заплатить в валюте payer_currency.
    pub payer_amount: Money,
    /// Валюта, в которой платит плательщик.
    pub payer_currency: String,
    /// Сколько фактически получено (подтверждено), в валюте payer_currency.
    pub payment_amount: Money,
    /// Глобальный номер события: в пределах одного объекта больший номер новее, меньший —
    /// опоздавшая доставка, её нужно отбросить. У репетиции (test: true) всегда 0.
    pub sequence: i64,
    /// Статус платежа — тот же литерал, что в /v1/payment/info и фильтре истории.
    pub status: PaymentStatus,
    /// Хеш транзакции, которой пришёл платёж (пусто, пока платежа нет).
    pub txid: String,
    /// Вид события: payment | payout | wallet | conversion — какое тело пришло.
    pub r#type: String,
    /// Идентификатор платежа.
    pub uuid: String,
    /// Есть только у репетиции (/v1/test-webhook/*, /v1/payment/testing-webhook) и всегда true —
    /// внутри подписи. Боевое событие этого поля не несёт никогда: тело с test: true обработчик
    /// обязан игнорировать, даже если подпись верна.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub test: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PaymentWebhook {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PaymentWebhook");
        d.field("additional_data", &self.additional_data);
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("event_at", &self.event_at);
        d.field("is_final", &self.is_final);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("payer_address", &self.payer_address);
        d.field(
            "payer_address_is_refundable",
            &self.payer_address_is_refundable,
        );
        d.field("payer_amount", &self.payer_amount);
        d.field("payer_currency", &self.payer_currency);
        d.field("payment_amount", &self.payment_amount);
        d.field("sequence", &self.sequence);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("type", &self.r#type);
        d.field("uuid", &self.uuid);
        d.field("test", &self.test);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutBatchRequest {
    /// Массив от 1 до 5000 элементов — те же поля, что у POST /v1/payout; order_id у каждого
    /// элемента обязателен и служит ключом идемпотентности: повтор вернёт уже созданную выплату.
    pub payouts: Vec<PayoutRequest>,
    /// Что делать при ошибке элемента: continue (по умолчанию) — обрабатывать остальные; stop —
    /// прекратить обработку после первой ошибки.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub on_error: Option<BatchOnError>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutBatchRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutBatchRequest");
        d.field("payouts", &self.payouts);
        d.field("on_error", &self.on_error);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutBatchRequest {
    /// A `PayoutBatchRequest` with the required fields; the optional ones start as `None`.
    pub fn new(payouts: Vec<PayoutRequest>) -> Self {
        Self {
            payouts,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutCalculateRequest {
    /// Сумма выплаты, строкой.
    pub amount: Money,
    /// Актив выплаты (USDT, BTC, …).
    pub currency: String,
    /// true — комиссия списывается с баланса поверх суммы (получатель получит ровно amount); false
    /// — из суммы выплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_subtract: Option<bool>,
    /// Сеть выплаты; обязательна, если актив живёт в нескольких сетях.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutCalculateRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutCalculateRequest");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("is_subtract", &self.is_subtract);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutCalculateRequest {
    /// A `PayoutCalculateRequest` with the required fields; the optional ones start as `None`.
    pub fn new(amount: impl Into<Money>, currency: impl Into<String>) -> Self {
        Self {
            amount: amount.into(),
            currency: currency.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutCalculation {
    /// Актив выплаты.
    pub currency: String,
    /// Кто платит комиссию: gateway, merchant или recipient.
    pub fee_bearer: PayoutFeeBearer,
    /// exact — комиссия договорная (шлюз её берёт на себя); estimated — оценка оракула.
    pub fee_type: FeeType,
    /// Сеть — как пришла в запросе.
    pub network: String,
    /// Сколько спишется с баланса; null — неизвестно (комиссию не оценить).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount: Option<Money>,
    /// Сетевая комиссия; null — не оценить сейчас.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commission: Option<Money>,
    /// Сколько получит адрес; null — неизвестно.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_amount: Option<Money>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutCalculation {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutCalculation");
        d.field("currency", &self.currency);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("fee_type", &self.fee_type);
        d.field("network", &self.network);
        d.field("amount", &self.amount);
        d.field("commission", &self.commission);
        d.field("payer_amount", &self.payer_amount);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutClaimLockedView {
    /// Получить можно сейчас: ссылка оплачена и не истекла.
    pub claimable: bool,
    /// До какого момента ссылку можно получить (UTC).
    pub expires_at: String,
    /// Всегда true: суммы и сеть покажутся после кода в заголовке X-Claim-Passcode.
    pub passcode_required: bool,
    /// Состояние ссылки.
    pub status: PayoutLinkStatus,
    /// Заголовок от отправителя.
    pub title: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutClaimLockedView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutClaimLockedView");
        d.field("claimable", &self.claimable);
        d.field("expires_at", &self.expires_at);
        d.field("passcode_required", &self.passcode_required);
        d.field("status", &self.status);
        d.field("title", &self.title);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutClaimView {
    /// Сумма ссылки — обещание получателю.
    pub amount: Money,
    /// Получить можно сейчас: ссылка оплачена и не истекла.
    pub claimable: bool,
    /// Актив выплаты.
    pub currency: String,
    /// До какого момента ссылку можно получить (UTC).
    pub expires_at: String,
    /// Кто платит сетевую комиссию.
    pub fee_bearer: PayoutLinkFeeBearer,
    /// exact — комиссия зафиксирована; estimated — оценка по текущей сети.
    pub fee_type: FeeType,
    /// Сеть выплаты.
    pub network: String,
    /// Сообщение от отправителя.
    pub note: String,
    /// Состояние ссылки.
    pub status: PayoutLinkStatus,
    /// Заголовок от отправителя.
    pub title: String,
    /// Сетевая комиссия; null — оценить сейчас нельзя (ноль означал бы бесплатное получение).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commission: Option<Money>,
    /// Сколько дойдёт получателю; null — сказать нельзя (комиссия не оценена или съела сумму).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_amount: Option<Money>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutClaimView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutClaimView");
        d.field("amount", &self.amount);
        d.field("claimable", &self.claimable);
        d.field("currency", &self.currency);
        d.field("expires_at", &self.expires_at);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("fee_type", &self.fee_type);
        d.field("network", &self.network);
        d.field("note", &self.note);
        d.field("status", &self.status);
        d.field("title", &self.title);
        d.field("commission", &self.commission);
        d.field("payer_amount", &self.payer_amount);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutClaimed {
    /// Адрес получателя.
    pub address: String,
    /// Сумма ссылки — обещание получателю.
    pub amount: Money,
    /// Актив выплаты.
    pub currency: String,
    /// Кто платит сетевую комиссию.
    pub fee_bearer: PayoutLinkFeeBearer,
    /// exact — комиссия зафиксирована; estimated — оценка по текущей сети.
    pub fee_type: FeeType,
    /// Сеть выплаты.
    pub network: String,
    /// Выплата получателю.
    pub payout_id: String,
    /// Состояние ссылки после получения.
    pub status: PayoutLinkStatus,
    /// Сетевая комиссия; null — оценить сейчас нельзя (ноль означал бы бесплатное получение).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commission: Option<Money>,
    /// Сколько дойдёт получателю; null — сказать нельзя (комиссия не оценена или съела сумму).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_amount: Option<Money>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutClaimed {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutClaimed");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("fee_type", &self.fee_type);
        d.field("network", &self.network);
        d.field("payout_id", &self.payout_id);
        d.field("status", &self.status);
        d.field("commission", &self.commission);
        d.field("payer_amount", &self.payer_amount);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutFeeResult {
    /// true — проект задал настройку сам; false — действует умолчание шлюза.
    pub configured: bool,
    /// Действующее значение: настройка проекта, а без неё — умолчание шлюза.
    pub fee_on_recipient: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutFeeResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutFeeResult");
        d.field("configured", &self.configured);
        d.field("fee_on_recipient", &self.fee_on_recipient);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutInfoResult {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency, списанная с вашего баланса.
    pub amount: Money,
    /// true — выплата ждёт подтверждения (внутренние сценарии; по API-ключу всегда false).
    pub approval_required: bool,
    /// Удержанная сетевая комиссия, в валюте выплаты. 0 — комиссию поглотил шлюз.
    pub commission: Money,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Код валюты выплаты.
    pub currency: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать получателю. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Кто заплатил сетевую комиссию: gateway — шлюз поглотил её (commission = 0); merchant — сумма
    /// списания увеличена на комиссию, получатель получает запрошенное целиком (is_subtract=true,
    /// выплатная ссылка с fee_bearer=merchant); recipient — комиссия удержана из выплаты,
    /// получателю приходит меньше запрошенного.
    pub fee_bearer: PayoutFeeBearer,
    /// true — статус финальный (confirmed / failed / cancelled).
    pub is_final: bool,
    /// true — это возврат платежа, а не обычная выплата.
    pub is_refund: bool,
    /// Тег/мемо назначения, переданный при создании (TON Jetton, memo-биржи). Пусто — без мемо.
    pub memo: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Сколько реально уходит получателю на адрес: amount − commission.
    pub payer_amount: Money,
    /// api (через интеграцию) | manual (из кабинета).
    pub source: PayoutSource,
    /// Статус выплаты: pending (создана, ждёт) | approved (одобрена) | awaiting_cosign (ждёт второй
    /// подписи) | broadcasting (отправляется) | sent (отправлена, ждёт подтверждений) | confirmed
    /// (подтверждена — готово) | failed | cancelled. Значение можно передать обратно в фильтр
    /// истории как есть.
    pub status: PayoutStatus,
    /// Хеш транзакции в блокчейне (появляется после отправки).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Причина сбоя выплаты человеческим текстом; null — сбоя нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<String>,
    /// Машинный код причины; null — сбоя нет.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_code: Option<String>,
    /// Ваш номер (reference) выплаты. У возврата — null: возврат не имеет вашего идентификатора,
    /// см. payment_order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Ваш order_id платежа, по которому сделан возврат (null у обычной выплаты). У возврата
    /// собственного order_id нет — он приходит null, а сверять возврат с заказом нужно по этому
    /// полю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payment_order_id: Option<String>,
    /// Идентификатор возвращаемого платежа (null, если это не возврат).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_for: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutInfoResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutInfoResult");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("approval_required", &self.approval_required);
        d.field("commission", &self.commission);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("is_final", &self.is_final);
        d.field("is_refund", &self.is_refund);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("source", &self.source);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("uuid", &self.uuid);
        d.field("error", &self.error);
        d.field("error_code", &self.error_code);
        d.field("order_id", &self.order_id);
        d.field("payment_order_id", &self.payment_order_id);
        d.field("refund_for", &self.refund_for);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutItem {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency, списанная с вашего баланса.
    pub amount: Money,
    /// true — выплата ждёт подтверждения (внутренние сценарии; по API-ключу всегда false).
    pub approval_required: bool,
    /// Удержанная сетевая комиссия, в валюте выплаты. 0 — комиссию поглотил шлюз.
    pub commission: Money,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Код валюты выплаты.
    pub currency: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать получателю. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Кто заплатил сетевую комиссию: gateway — шлюз поглотил её (commission = 0); merchant — сумма
    /// списания увеличена на комиссию, получатель получает запрошенное целиком (is_subtract=true,
    /// выплатная ссылка с fee_bearer=merchant); recipient — комиссия удержана из выплаты,
    /// получателю приходит меньше запрошенного.
    pub fee_bearer: PayoutFeeBearer,
    /// true — статус финальный (confirmed / failed / cancelled).
    pub is_final: bool,
    /// true — это возврат платежа, а не обычная выплата.
    pub is_refund: bool,
    /// Тег/мемо назначения, переданный при создании (TON Jetton, memo-биржи). Пусто — без мемо.
    pub memo: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Сколько реально уходит получателю на адрес: amount − commission.
    pub payer_amount: Money,
    /// api (через интеграцию) | manual (из кабинета).
    pub source: PayoutSource,
    /// Статус выплаты: pending (создана, ждёт) | approved (одобрена) | awaiting_cosign (ждёт второй
    /// подписи) | broadcasting (отправляется) | sent (отправлена, ждёт подтверждений) | confirmed
    /// (подтверждена — готово) | failed | cancelled. Значение можно передать обратно в фильтр
    /// истории как есть.
    pub status: PayoutStatus,
    /// Хеш транзакции в блокчейне (появляется после отправки).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Конверсия, сделанная по пути выплаты; нет ключа — конверсии не было.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub convert: Option<BTreeMap<String, serde_json::Value>>,
    /// Ваш номер (reference) выплаты. У возврата — null: возврат не имеет вашего идентификатора,
    /// см. payment_order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Ваш order_id платежа, по которому сделан возврат (null у обычной выплаты). У возврата
    /// собственного order_id нет — он приходит null, а сверять возврат с заказом нужно по этому
    /// полю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payment_order_id: Option<String>,
    /// Идентификатор возвращаемого платежа (null, если это не возврат).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_for: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutItem");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("approval_required", &self.approval_required);
        d.field("commission", &self.commission);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("is_final", &self.is_final);
        d.field("is_refund", &self.is_refund);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("source", &self.source);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("uuid", &self.uuid);
        d.field("convert", &self.convert);
        d.field("order_id", &self.order_id);
        d.field("payment_order_id", &self.payment_order_id);
        d.field("refund_for", &self.refund_for);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkBatchItem {
    /// Сумма в currency, строкой; больше нуля
    pub amount: Money,
    /// Крипто-актив выплаты (USDT, BTC, …); фиат невозможен
    pub currency: String,
    /// Сеть выплаты получателю (tron, bitcoin, …)
    pub network: String,
    /// Ваш ключ дедупликации ссылки, уникальный на мерчанта: повтор с тем же reference не
    /// зарезервирует деньги второй раз. В одиночном POST /v1/payout/link необязателен — без него
    /// ключом становится заголовок Idempotency-Key, а без обоих запрос отвергается
    /// (payoutlink.idempotency_required). В пачке POST /v1/payout/link/batch обязателен у каждой
    /// ссылки: Idempotency-Key пачки на элементы не переносится
    pub reference: String,
    /// Если задан — получателю уходит письмо с кнопкой «Получить средства»; сбой доставки не
    /// отменяет создание ссылки
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// Срок жизни ссылки в секундах, клампится в диапазон 3600–2592000 (час–30 суток); без поля или
    /// при 0 ссылка живёт 1 час, а не максимум — задавайте явно
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_in_seconds: Option<i64>,
    /// Кто платит сетевую комиссию: "recipient" (по умолчанию — вычитается из суммы, получателю
    /// придёт меньше) или "merchant" (резервируется сумма плюс комиссия, получателю придёт ровно
    /// amount)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_bearer: Option<PayoutLinkFeeBearer>,
    /// Сообщение получателю (видно на странице получения и в письме)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub note: Option<String>,
    /// Код получения — второй фактор к ссылке: "auto" — сгенерируем и вернём ОДИН раз в ответе,
    /// либо свой (6–64 видимых символа), пусто — без кода. Код передавайте получателю ОТДЕЛЬНЫМ от
    /// ссылки каналом (в письмо он не кладётся); после 10 неверных вводов ссылка запирается.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub passcode: Option<String>,
    /// Заголовок — виден получателю на странице получения
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkBatchItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkBatchItem");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("reference", &self.reference);
        d.field("email", &self.email);
        d.field("expires_in_seconds", &self.expires_in_seconds);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("note", &self.note);
        d.field("passcode", &self.passcode);
        d.field("title", &self.title);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkBatchRequest {
    /// До 500 ссылок за вызов; каждая проходит или падает независимо, ответ выровнен по индексам
    /// запроса. reference обязателен у каждой.
    pub items: Vec<PayoutLinkBatchItem>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkBatchRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkBatchRequest");
        d.field("items", &self.items);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutLinkBatchRequest {
    /// A `PayoutLinkBatchRequest` with the required fields; the optional ones start as `None`.
    pub fn new(items: Vec<PayoutLinkBatchItem>) -> Self {
        Self {
            items,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkBatchResult {
    /// Элементы в порядке запроса; result — ответ одиночного POST /v1/payout/link.
    pub items: Vec<PayoutLinkBatchResultItemsItem>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkBatchResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkBatchResult");
        d.field("items", &self.items);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkBatchResultItemsItem {
    /// Номер элемента в запросе.
    pub idx: i64,
    /// Элемент выполнен.
    pub ok: bool,
    /// Машинный код отказа; есть при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error_code: Option<String>,
    /// HTTP-статус, которым ответил бы одиночный вызов; есть при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub http_status: Option<i64>,
    /// Текст отказа; есть при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub message: Option<String>,
    /// order_id элемента, если он был в запросе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Результат одиночного вызова; есть при ok=true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub result: Option<PayoutLinkCreated>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkBatchResultItemsItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkBatchResultItemsItem");
        d.field("idx", &self.idx);
        d.field("ok", &self.ok);
        d.field("error_code", &self.error_code);
        d.field("http_status", &self.http_status);
        d.field("message", &self.message);
        d.field("order_id", &self.order_id);
        d.field("result", &self.result);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkChequeRequest {
    /// Секрет получения из ответа создания выплатной ссылки. Хранится только хешем и повторно не
    /// выдаётся — чек можно напечатать, лишь пока токен у вас.
    pub claim_token: String,
    /// Язык документа — один из 41 поддерживаемого кода (en по умолчанию); полный список — в ошибке
    /// document.unknown_lang.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub lang: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkChequeRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkChequeRequest");
        d.field("claim_token", &self.claim_token);
        d.field("lang", &self.lang);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutLinkChequeRequest {
    /// A `PayoutLinkChequeRequest` with the required fields; the optional ones start as `None`.
    pub fn new(claim_token: impl Into<String>) -> Self {
        Self {
            claim_token: claim_token.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkCreated {
    /// Сумма ссылки — обещание получателю.
    pub amount: Money,
    /// Секрет ссылки получения; выдаётся один раз и хранится только хешем.
    pub claim_token: String,
    /// Страница получения; пусто, если публичный адрес не настроен.
    pub claim_url: String,
    /// Когда создана (UTC).
    pub created_at: String,
    /// Актив выплаты.
    pub currency: String,
    /// До какого момента ссылку можно получить (UTC).
    pub expires_at: String,
    /// Кто платит сетевую комиссию.
    pub fee_bearer: PayoutLinkFeeBearer,
    /// exact — комиссия зафиксирована; estimated — оценка по текущей сети.
    pub fee_type: FeeType,
    /// Идентификатор ссылки.
    pub link_id: String,
    /// Сеть выплаты.
    pub network: String,
    /// Сообщение получателю.
    pub note: String,
    /// Получение требует кода.
    pub passcode_protected: bool,
    /// Состояние ссылки.
    pub status: PayoutLinkStatus,
    /// Заголовок, видный получателю.
    pub title: String,
    /// Пачка, в которой создана ссылка.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub batch_id: Option<String>,
    /// Адрес, который указал получатель.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub claim_address: Option<String>,
    /// Сетевая комиссия; null — оценить сейчас нельзя (ноль означал бы бесплатное получение).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commission: Option<Money>,
    /// Адрес, на который ушло письмо получателю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// Сгенерированный код получения (passcode=auto); выдаётся один раз.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub passcode: Option<String>,
    /// Сколько дойдёт получателю; null — сказать нельзя (комиссия не оценена или съела сумму).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_amount: Option<Money>,
    /// Выплата, порождённая получением; есть у полученной ссылки.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payout_id: Option<String>,
    /// Ваш ключ дедупликации.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reference: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkCreated {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkCreated");
        d.field("amount", &self.amount);
        d.field("claim_token", &self.claim_token);
        d.field("claim_url", &self.claim_url);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("expires_at", &self.expires_at);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("fee_type", &self.fee_type);
        d.field("link_id", &self.link_id);
        d.field("network", &self.network);
        d.field("note", &self.note);
        d.field("passcode_protected", &self.passcode_protected);
        d.field("status", &self.status);
        d.field("title", &self.title);
        d.field("batch_id", &self.batch_id);
        d.field("claim_address", &self.claim_address);
        d.field("commission", &self.commission);
        d.field("email", &self.email);
        d.field("passcode", &self.passcode);
        d.field("payer_amount", &self.payer_amount);
        d.field("payout_id", &self.payout_id);
        d.field("reference", &self.reference);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkIDRequest {
    /// Идентификатор выплатной ссылки (link_id из ответа создания).
    pub link_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkIDRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkIDRequest");
        d.field("link_id", &self.link_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutLinkIDRequest {
    /// A `PayoutLinkIDRequest` with the required fields; the optional ones start as `None`.
    pub fn new(link_id: impl Into<String>) -> Self {
        Self {
            link_id: link_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkItem {
    /// Сумма в currency, строкой; больше нуля
    pub amount: Money,
    /// Крипто-актив выплаты (USDT, BTC, …); фиат невозможен
    pub currency: String,
    /// Сеть выплаты получателю (tron, bitcoin, …)
    pub network: String,
    /// Если задан — получателю уходит письмо с кнопкой «Получить средства»; сбой доставки не
    /// отменяет создание ссылки
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// Срок жизни ссылки в секундах, клампится в диапазон 3600–2592000 (час–30 суток); без поля или
    /// при 0 ссылка живёт 1 час, а не максимум — задавайте явно
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub expires_in_seconds: Option<i64>,
    /// Кто платит сетевую комиссию: "recipient" (по умолчанию — вычитается из суммы, получателю
    /// придёт меньше) или "merchant" (резервируется сумма плюс комиссия, получателю придёт ровно
    /// amount)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub fee_bearer: Option<PayoutLinkFeeBearer>,
    /// Сообщение получателю (видно на странице получения и в письме)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub note: Option<String>,
    /// Код получения — второй фактор к ссылке: "auto" — сгенерируем и вернём ОДИН раз в ответе,
    /// либо свой (6–64 видимых символа), пусто — без кода. Код передавайте получателю ОТДЕЛЬНЫМ от
    /// ссылки каналом (в письмо он не кладётся); после 10 неверных вводов ссылка запирается.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub passcode: Option<String>,
    /// Ваш ключ дедупликации ссылки, уникальный на мерчанта: повтор с тем же reference не
    /// зарезервирует деньги второй раз. В одиночном POST /v1/payout/link необязателен — без него
    /// ключом становится заголовок Idempotency-Key, а без обоих запрос отвергается
    /// (payoutlink.idempotency_required). В пачке POST /v1/payout/link/batch обязателен у каждой
    /// ссылки: Idempotency-Key пачки на элементы не переносится
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reference: Option<String>,
    /// Заголовок — виден получателю на странице получения
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub title: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkItem");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("email", &self.email);
        d.field("expires_in_seconds", &self.expires_in_seconds);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("note", &self.note);
        d.field("passcode", &self.passcode);
        d.field("reference", &self.reference);
        d.field("title", &self.title);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutLinkItem {
    /// A `PayoutLinkItem` with the required fields; the optional ones start as `None`.
    pub fn new(
        amount: impl Into<Money>,
        currency: impl Into<String>,
        network: impl Into<String>,
    ) -> Self {
        Self {
            amount: amount.into(),
            currency: currency.into(),
            network: network.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkView {
    /// Сумма ссылки — обещание получателю.
    pub amount: Money,
    /// Когда создана (UTC).
    pub created_at: String,
    /// Актив выплаты.
    pub currency: String,
    /// До какого момента ссылку можно получить (UTC).
    pub expires_at: String,
    /// Кто платит сетевую комиссию.
    pub fee_bearer: PayoutLinkFeeBearer,
    /// exact — комиссия зафиксирована; estimated — оценка по текущей сети.
    pub fee_type: FeeType,
    /// Идентификатор ссылки.
    pub link_id: String,
    /// Сеть выплаты.
    pub network: String,
    /// Сообщение получателю.
    pub note: String,
    /// Получение требует кода.
    pub passcode_protected: bool,
    /// Состояние ссылки.
    pub status: PayoutLinkStatus,
    /// Заголовок, видный получателю.
    pub title: String,
    /// Пачка, в которой создана ссылка.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub batch_id: Option<String>,
    /// Адрес, который указал получатель.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub claim_address: Option<String>,
    /// Сетевая комиссия; null — оценить сейчас нельзя (ноль означал бы бесплатное получение).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub commission: Option<Money>,
    /// Адрес, на который ушло письмо получателю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// Сколько дойдёт получателю; null — сказать нельзя (комиссия не оценена или съела сумму).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payer_amount: Option<Money>,
    /// Выплата, порождённая получением; есть у полученной ссылки.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payout_id: Option<String>,
    /// Ваш ключ дедупликации.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reference: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkView");
        d.field("amount", &self.amount);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("expires_at", &self.expires_at);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("fee_type", &self.fee_type);
        d.field("link_id", &self.link_id);
        d.field("network", &self.network);
        d.field("note", &self.note);
        d.field("passcode_protected", &self.passcode_protected);
        d.field("status", &self.status);
        d.field("title", &self.title);
        d.field("batch_id", &self.batch_id);
        d.field("claim_address", &self.claim_address);
        d.field("commission", &self.commission);
        d.field("email", &self.email);
        d.field("payer_amount", &self.payer_amount);
        d.field("payout_id", &self.payout_id);
        d.field("reference", &self.reference);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutLinkViewList {
    /// Записи этой страницы.
    pub items: Vec<PayoutLinkView>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutLinkViewList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutLinkViewList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutRequest {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency.
    pub amount: Money,
    /// Код валюты (например USDT).
    pub currency: String,
    /// Ваш номер выплаты; ключ идемпотентности.
    pub order_id: String,
    /// Профинансировать выплату конвертацией баланса. Только USDT → currency.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from_currency: Option<String>,
    /// Кто платит сетевую комиссию: true — с баланса списывается amount+fee, получатель получает
    /// amount; false — получатель получает amount-fee; не передано — fee-config проекта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_subtract: Option<bool>,
    /// Тег/мемо назначения (TON Jetton). Максимум 120 символов.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub memo: Option<String>,
    /// Сеть (tron, ethereum, …). Обязательна для монет с несколькими сетями.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Метка происхождения: api (по умолчанию) или manual.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<String>,
    /// Свой URL вебхука для этой выплаты (проходит SSRF-проверку). Требует зарегистрированного
    /// эндпоинта (POST /v1/webhooks): доставка подписывается его секретом.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_callback: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutRequest");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("order_id", &self.order_id);
        d.field("from_currency", &self.from_currency);
        d.field("is_subtract", &self.is_subtract);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("source", &self.source);
        d.field("url_callback", &self.url_callback);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutRequest {
    /// A `PayoutRequest` with the required fields; the optional ones start as `None`.
    pub fn new(
        address: impl Into<String>,
        amount: impl Into<Money>,
        currency: impl Into<String>,
        order_id: impl Into<String>,
    ) -> Self {
        Self {
            address: address.into(),
            amount: amount.into(),
            currency: currency.into(),
            order_id: order_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutValidateRequest {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency.
    pub amount: Money,
    /// Код валюты (например USDT).
    pub currency: String,
    /// Профинансировать выплату конвертацией баланса. Только USDT → currency.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from_currency: Option<String>,
    /// Кто платит сетевую комиссию: true — с баланса списывается amount+fee, получатель получает
    /// amount; false — получатель получает amount-fee; не передано — fee-config проекта.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub is_subtract: Option<bool>,
    /// Тег/мемо назначения (TON Jetton). Максимум 120 символов.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub memo: Option<String>,
    /// Сеть (tron, ethereum, …). Обязательна для монет с несколькими сетями.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Ваш номер выплаты; ключ идемпотентности.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Метка происхождения: api (по умолчанию) или manual.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub source: Option<String>,
    /// Свой URL вебхука для этой выплаты (проходит SSRF-проверку). Требует зарегистрированного
    /// эндпоинта (POST /v1/webhooks): доставка подписывается его секретом.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url_callback: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutValidateRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutValidateRequest");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("from_currency", &self.from_currency);
        d.field("is_subtract", &self.is_subtract);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("source", &self.source);
        d.field("url_callback", &self.url_callback);
        d.extra(&self.extra);
        d.finish()
    }
}

impl PayoutValidateRequest {
    /// A `PayoutValidateRequest` with the required fields; the optional ones start as `None`.
    pub fn new(
        address: impl Into<String>,
        amount: impl Into<Money>,
        currency: impl Into<String>,
    ) -> Self {
        Self {
            address: address.into(),
            amount: amount.into(),
            currency: currency.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutValidateResult {
    /// Сколько спишется с баланса.
    pub amount: Money,
    /// Сетевая комиссия.
    pub commission: Money,
    /// Валюта выплаты.
    pub currency: String,
    /// Кто платит сетевую комиссию.
    pub fee_bearer: PayoutFeeBearer,
    /// Что именно проверено по балансу и что проверится при исполнении.
    pub maturity_note: String,
    /// Сеть выплаты в каноническом написании.
    pub network: String,
    /// Сколько дойдёт получателю.
    pub payer_amount: Money,
    /// Всегда true: не прошедшая проверка отвечает ошибкой с кодом причины.
    pub valid: bool,
    /// Валюта, конвертацией которой профинансируется выплата (from_currency); есть только у такой
    /// выплаты.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub funded_by: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutValidateResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutValidateResult");
        d.field("amount", &self.amount);
        d.field("commission", &self.commission);
        d.field("currency", &self.currency);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("maturity_note", &self.maturity_note);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("valid", &self.valid);
        d.field("funded_by", &self.funded_by);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutView {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency, списанная с вашего баланса.
    pub amount: Money,
    /// true — выплата ждёт подтверждения (внутренние сценарии; по API-ключу всегда false).
    pub approval_required: bool,
    /// Удержанная сетевая комиссия, в валюте выплаты. 0 — комиссию поглотил шлюз.
    pub commission: Money,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Код валюты выплаты.
    pub currency: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать получателю. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Кто заплатил сетевую комиссию: gateway — шлюз поглотил её (commission = 0); merchant — сумма
    /// списания увеличена на комиссию, получатель получает запрошенное целиком (is_subtract=true,
    /// выплатная ссылка с fee_bearer=merchant); recipient — комиссия удержана из выплаты,
    /// получателю приходит меньше запрошенного.
    pub fee_bearer: PayoutFeeBearer,
    /// true — статус финальный (confirmed / failed / cancelled).
    pub is_final: bool,
    /// true — это возврат платежа, а не обычная выплата.
    pub is_refund: bool,
    /// Тег/мемо назначения, переданный при создании (TON Jetton, memo-биржи). Пусто — без мемо.
    pub memo: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Сколько реально уходит получателю на адрес: amount − commission.
    pub payer_amount: Money,
    /// api (через интеграцию) | manual (из кабинета).
    pub source: PayoutSource,
    /// Статус выплаты: pending (создана, ждёт) | approved (одобрена) | awaiting_cosign (ждёт второй
    /// подписи) | broadcasting (отправляется) | sent (отправлена, ждёт подтверждений) | confirmed
    /// (подтверждена — готово) | failed | cancelled. Значение можно передать обратно в фильтр
    /// истории как есть.
    pub status: PayoutStatus,
    /// Хеш транзакции в блокчейне (появляется после отправки).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Ваш номер (reference) выплаты. У возврата — null: возврат не имеет вашего идентификатора,
    /// см. payment_order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Ваш order_id платежа, по которому сделан возврат (null у обычной выплаты). У возврата
    /// собственного order_id нет — он приходит null, а сверять возврат с заказом нужно по этому
    /// полю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payment_order_id: Option<String>,
    /// Идентификатор возвращаемого платежа (null, если это не возврат).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_for: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutView");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("approval_required", &self.approval_required);
        d.field("commission", &self.commission);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("is_final", &self.is_final);
        d.field("is_refund", &self.is_refund);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("source", &self.source);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("uuid", &self.uuid);
        d.field("order_id", &self.order_id);
        d.field("payment_order_id", &self.payment_order_id);
        d.field("refund_for", &self.refund_for);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutViewList {
    /// Записи этой страницы.
    pub items: Vec<PayoutView>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutViewList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutViewList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

/// Приходит на каждом переходе выплаты. Тело — тот же объект, что отвечают ручки выплат. Возврат
/// платежа — это выплата с is_refund = true: его события тоже payout.*, сверять с платежом по
/// refund_for и payment_order_id.
#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PayoutWebhook {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency, списанная с вашего баланса.
    pub amount: Money,
    /// true — выплата ждёт подтверждения (внутренние сценарии; по API-ключу всегда false).
    pub approval_required: bool,
    /// Удержанная сетевая комиссия, в валюте выплаты. 0 — комиссию поглотил шлюз.
    pub commission: Money,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Код валюты выплаты.
    pub currency: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать получателю. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Когда событие произошло, UTC с миллисекундами (ISO 8601).
    pub event_at: String,
    /// Кто заплатил сетевую комиссию: gateway — шлюз поглотил её (commission = 0); merchant — сумма
    /// списания увеличена на комиссию, получатель получает запрошенное целиком (is_subtract=true,
    /// выплатная ссылка с fee_bearer=merchant); recipient — комиссия удержана из выплаты,
    /// получателю приходит меньше запрошенного.
    pub fee_bearer: PayoutFeeBearer,
    /// true — статус финальный (confirmed / failed / cancelled).
    pub is_final: bool,
    /// true — это возврат платежа, а не обычная выплата.
    pub is_refund: bool,
    /// Тег/мемо назначения, переданный при создании (TON Jetton, memo-биржи). Пусто — без мемо.
    pub memo: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Сколько реально уходит получателю на адрес: amount − commission.
    pub payer_amount: Money,
    /// Глобальный номер события: в пределах одного объекта больший номер новее, меньший —
    /// опоздавшая доставка, её нужно отбросить. У репетиции (test: true) всегда 0.
    pub sequence: i64,
    /// api (через интеграцию) | manual (из кабинета).
    pub source: PayoutSource,
    /// Статус выплаты: pending (создана, ждёт) | approved (одобрена) | awaiting_cosign (ждёт второй
    /// подписи) | broadcasting (отправляется) | sent (отправлена, ждёт подтверждений) | confirmed
    /// (подтверждена — готово) | failed | cancelled. Значение можно передать обратно в фильтр
    /// истории как есть.
    pub status: PayoutStatus,
    /// Хеш транзакции в блокчейне (появляется после отправки).
    pub txid: String,
    /// Вид события: payment | payout | wallet | conversion — какое тело пришло.
    pub r#type: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Ваш номер (reference) выплаты. У возврата — null: возврат не имеет вашего идентификатора,
    /// см. payment_order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Ваш order_id платежа, по которому сделан возврат (null у обычной выплаты). У возврата
    /// собственного order_id нет — он приходит null, а сверять возврат с заказом нужно по этому
    /// полю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payment_order_id: Option<String>,
    /// Идентификатор возвращаемого платежа (null, если это не возврат).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_for: Option<String>,
    /// Есть только у репетиции (/v1/test-webhook/*, /v1/payment/testing-webhook) и всегда true —
    /// внутри подписи. Боевое событие этого поля не несёт никогда: тело с test: true обработчик
    /// обязан игнорировать, даже если подпись верна.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub test: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PayoutWebhook {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PayoutWebhook");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("approval_required", &self.approval_required);
        d.field("commission", &self.commission);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("event_at", &self.event_at);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("is_final", &self.is_final);
        d.field("is_refund", &self.is_refund);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("sequence", &self.sequence);
        d.field("source", &self.source);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("type", &self.r#type);
        d.field("updated_at", &self.updated_at);
        d.field("uuid", &self.uuid);
        d.field("order_id", &self.order_id);
        d.field("payment_order_id", &self.payment_order_id);
        d.field("refund_for", &self.refund_for);
        d.field("test", &self.test);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PricingCurrency {
    /// Код для поля currency при создании счёта.
    pub currency: String,
    /// Знаков после запятой.
    pub decimals: i64,
    /// Фиат: счёт в нём выставляется, но оплачивается монетой.
    pub fiat: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PricingCurrency {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PricingCurrency");
        d.field("currency", &self.currency);
        d.field("decimals", &self.decimals);
        d.field("fiat", &self.fiat);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PublicPayResult {
    /// Адрес, на который клиент отправляет деньги. На XRP это классический r-адрес ОБЩЕГО кошелька
    /// — платёж обязан нести destination_tag, иначе сеть его отклонит.
    pub address: String,
    /// Только XLM: те же реквизиты одной строкой — muxed-адрес M… (SEP-23), адрес и memo вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_muxed: String,
    /// QR-код адреса как PNG data:-URI — можно сразу в \<img src\>. На XRP кодирует X-address
    /// (адрес+тег одной строкой).
    pub address_qr_code: String,
    /// Только XRP: те же реквизиты одной строкой в формате X-address (XLS-5) — адрес и тег вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_xaddress: String,
    /// Сумма к оплате в валюте цены (например, в USD).
    pub amount: Money,
    /// Сколько уже подтверждённо оплачено, в крипте оплаты; всегда строка (0, если ничего не
    /// пришло). Пусто, пока валюта оплаты не выбрана (счёт без валюты).
    pub amount_paid: String,
    /// Сколько ещё осталось доплатить (к оплате − оплачено); 0, если хватает. Пусто, пока валюта
    /// оплаты не выбрана (счёт без валюты).
    pub amount_remaining: String,
    /// Текущее число подтверждений входящего платежа.
    pub confirmations: i64,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Валюта цены: фиат (USD, EUR, RUB, JPY… — см. pricing_currencies) или монета. Говорит,
    /// сколько счёт СТОИТ, а не чем за него платят (это payer_currency).
    pub currency: String,
    /// Только XRP: числовой destination tag, который клиент ОБЯЗАН указать в переводе (поле
    /// «тег/memo получателя» на бирже или в кошельке). Пусто на остальных сетях.
    pub destination_tag: String,
    /// Когда истекает счёт (ISO 8601, как и все временные поля).
    pub expired_at: String,
    /// Можно ли сейчас оплатить картой через он-рамп.
    pub fiat_purchase_available: bool,
    /// true — статус финальный, больше не изменится.
    pub is_final: bool,
    /// true — это валюто-агностичная ссылка, клиент ещё не выбрал валюту/сеть.
    pub is_multi: bool,
    /// Только XLM (Stellar): числовой memo (тип ID), который клиент ОБЯЗАН указать в переводе —
    /// поле «memo» на бирже или в кошельке. Пусто на остальных сетях.
    pub memo: String,
    /// Ваша скидка или наценка для ВЫБРАННОГО способа оплаты, в валюте оплаты: на столько
    /// сдвинулась сумма плательщика из-за настройки по этой монете и сети. Положительное —
    /// плательщик платит МЕНЬШЕ (скидка), отрицательное — больше (наценка). Пусто, если настройки
    /// для метода нет.
    pub method_adjustment: String,
    /// Та же скидка/наценка в базисных пунктах (так она переживает переоценку курса). Знак тот же,
    /// что в настройке скидок: ПЛЮС — скидка, МИНУС — наценка.
    pub method_adjustment_bps: i64,
    /// Сеть блокчейна (например, tron).
    pub network: String,
    /// Сетевая надбавка плательщика в валюте оплаты: стоимость сбора депозита в выбранной сети
    /// (активация адреса, если адрес новый, плюс энергия/газ с запасом), зафиксированная при выборе
    /// сети. Пусто до выбора сети; 0, если надбавка выключена.
    pub network_surcharge: String,
    /// Та же надбавка в базисных пунктах от суммы к оплате (так она переживает переоценку курса).
    pub network_surcharge_bps: i64,
    /// Ваш номер заказа, который вы передали при создании.
    pub order_id: String,
    /// Сколько нужно отправить в крипте оплаты. Пусто, пока валюта оплаты не выбрана (счёт без
    /// валюты).
    pub payer_amount: String,
    /// Валюта, в которой платит клиент (например, USDT). Пусто у валюто-агностичного счёта
    /// (is_multi), пока клиент не выбрал монету — валюты расчёта у него ещё нет.
    pub payer_currency: String,
    /// До какого момента действует зафиксированный payer_amount (ISO 8601; окно ~5 мин, после него
    /// страница оплаты перекотирует счёт). Пусто, когда перекотировки уже не будет: валюта не
    /// выбрана, депозит замечен, счёт вышел из created или истёк — сумма зафиксирована навсегда.
    pub rate_expires_at: String,
    /// Сколько подтверждений нужно для зачисления (зависит от суммы и сети).
    pub required_confirmations: i64,
    /// Статус: select (клиент выбирает валюту) | created (ждём оплату) | confirm_check (видим
    /// оплату, ждём подтверждений; при amount_remaining \> 0 — частичная, ждём остаток) | paid
    /// (оплачено) | paid_over (переплата) | wrong_amount (недоплата, срок вышел) | expired
    /// (просрочен) | cancelled (отменён) | under_review (поступление задержано на проверке,
    /// разбирает оператор).
    pub status: PaymentStatus,
    /// Хеш входящей транзакции (когда замечена).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Ссылка на готовую страницу оплаты.
    pub url: String,
    /// Ссылка «вернуться в магазин» до оплаты.
    pub url_return: String,
    /// Куда перенаправить после успешной оплаты.
    pub url_success: String,
    /// Наш идентификатор платежа (используйте его в info/refund).
    pub uuid: String,
    /// Способы оплаты, из которых выбирает покупатель; есть только у счёта в статусе select.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accepted: Option<Vec<AcceptedMethod>>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PublicPayResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PublicPayResult");
        d.field("address", &self.address);
        d.field("address_muxed", &self.address_muxed);
        d.field("address_qr_code", &self.address_qr_code);
        d.field("address_xaddress", &self.address_xaddress);
        d.field("amount", &self.amount);
        d.field("amount_paid", &self.amount_paid);
        d.field("amount_remaining", &self.amount_remaining);
        d.field("confirmations", &self.confirmations);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("destination_tag", &self.destination_tag);
        d.field("expired_at", &self.expired_at);
        d.field("fiat_purchase_available", &self.fiat_purchase_available);
        d.field("is_final", &self.is_final);
        d.field("is_multi", &self.is_multi);
        d.field("memo", &self.memo);
        d.field("method_adjustment", &self.method_adjustment);
        d.field("method_adjustment_bps", &self.method_adjustment_bps);
        d.field("network", &self.network);
        d.field("network_surcharge", &self.network_surcharge);
        d.field("network_surcharge_bps", &self.network_surcharge_bps);
        d.field("order_id", &self.order_id);
        d.field("payer_amount", &self.payer_amount);
        d.field("payer_currency", &self.payer_currency);
        d.field("rate_expires_at", &self.rate_expires_at);
        d.field("required_confirmations", &self.required_confirmations);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("url", &self.url);
        d.field("url_return", &self.url_return);
        d.field("url_success", &self.url_success);
        d.field("uuid", &self.uuid);
        d.field("accepted", &self.accepted);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct PublicPaymentView {
    /// Адрес, на который клиент отправляет деньги. На XRP это классический r-адрес ОБЩЕГО кошелька
    /// — платёж обязан нести destination_tag, иначе сеть его отклонит.
    pub address: String,
    /// Только XLM: те же реквизиты одной строкой — muxed-адрес M… (SEP-23), адрес и memo вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_muxed: String,
    /// QR-код адреса как PNG data:-URI — можно сразу в \<img src\>. На XRP кодирует X-address
    /// (адрес+тег одной строкой).
    pub address_qr_code: String,
    /// Только XRP: те же реквизиты одной строкой в формате X-address (XLS-5) — адрес и тег вместе;
    /// его же кодирует QR. Пусто на остальных сетях.
    pub address_xaddress: String,
    /// Сумма к оплате в валюте цены (например, в USD).
    pub amount: Money,
    /// Сколько уже подтверждённо оплачено, в крипте оплаты; всегда строка (0, если ничего не
    /// пришло). Пусто, пока валюта оплаты не выбрана (счёт без валюты).
    pub amount_paid: String,
    /// Сколько ещё осталось доплатить (к оплате − оплачено); 0, если хватает. Пусто, пока валюта
    /// оплаты не выбрана (счёт без валюты).
    pub amount_remaining: String,
    /// Текущее число подтверждений входящего платежа.
    pub confirmations: i64,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Валюта цены: фиат (USD, EUR, RUB, JPY… — см. pricing_currencies) или монета. Говорит,
    /// сколько счёт СТОИТ, а не чем за него платят (это payer_currency).
    pub currency: String,
    /// Только XRP: числовой destination tag, который клиент ОБЯЗАН указать в переводе (поле
    /// «тег/memo получателя» на бирже или в кошельке). Пусто на остальных сетях.
    pub destination_tag: String,
    /// Когда истекает счёт (ISO 8601, как и все временные поля).
    pub expired_at: String,
    /// true — статус финальный, больше не изменится.
    pub is_final: bool,
    /// true — это валюто-агностичная ссылка, клиент ещё не выбрал валюту/сеть.
    pub is_multi: bool,
    /// Только XLM (Stellar): числовой memo (тип ID), который клиент ОБЯЗАН указать в переводе —
    /// поле «memo» на бирже или в кошельке. Пусто на остальных сетях.
    pub memo: String,
    /// Ваша скидка или наценка для ВЫБРАННОГО способа оплаты, в валюте оплаты: на столько
    /// сдвинулась сумма плательщика из-за настройки по этой монете и сети. Положительное —
    /// плательщик платит МЕНЬШЕ (скидка), отрицательное — больше (наценка). Пусто, если настройки
    /// для метода нет.
    pub method_adjustment: String,
    /// Та же скидка/наценка в базисных пунктах (так она переживает переоценку курса). Знак тот же,
    /// что в настройке скидок: ПЛЮС — скидка, МИНУС — наценка.
    pub method_adjustment_bps: i64,
    /// Сеть блокчейна (например, tron).
    pub network: String,
    /// Сетевая надбавка плательщика в валюте оплаты: стоимость сбора депозита в выбранной сети
    /// (активация адреса, если адрес новый, плюс энергия/газ с запасом), зафиксированная при выборе
    /// сети. Пусто до выбора сети; 0, если надбавка выключена.
    pub network_surcharge: String,
    /// Та же надбавка в базисных пунктах от суммы к оплате (так она переживает переоценку курса).
    pub network_surcharge_bps: i64,
    /// Ваш номер заказа, который вы передали при создании.
    pub order_id: String,
    /// Сколько нужно отправить в крипте оплаты. Пусто, пока валюта оплаты не выбрана (счёт без
    /// валюты).
    pub payer_amount: String,
    /// Валюта, в которой платит клиент (например, USDT). Пусто у валюто-агностичного счёта
    /// (is_multi), пока клиент не выбрал монету — валюты расчёта у него ещё нет.
    pub payer_currency: String,
    /// До какого момента действует зафиксированный payer_amount (ISO 8601; окно ~5 мин, после него
    /// страница оплаты перекотирует счёт). Пусто, когда перекотировки уже не будет: валюта не
    /// выбрана, депозит замечен, счёт вышел из created или истёк — сумма зафиксирована навсегда.
    pub rate_expires_at: String,
    /// Сколько подтверждений нужно для зачисления (зависит от суммы и сети).
    pub required_confirmations: i64,
    /// Статус: select (клиент выбирает валюту) | created (ждём оплату) | confirm_check (видим
    /// оплату, ждём подтверждений; при amount_remaining \> 0 — частичная, ждём остаток) | paid
    /// (оплачено) | paid_over (переплата) | wrong_amount (недоплата, срок вышел) | expired
    /// (просрочен) | cancelled (отменён) | under_review (поступление задержано на проверке,
    /// разбирает оператор).
    pub status: PaymentStatus,
    /// Хеш входящей транзакции (когда замечена).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Ссылка на готовую страницу оплаты.
    pub url: String,
    /// Ссылка «вернуться в магазин» до оплаты.
    pub url_return: String,
    /// Куда перенаправить после успешной оплаты.
    pub url_success: String,
    /// Наш идентификатор платежа (используйте его в info/refund).
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for PublicPaymentView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "PublicPaymentView");
        d.field("address", &self.address);
        d.field("address_muxed", &self.address_muxed);
        d.field("address_qr_code", &self.address_qr_code);
        d.field("address_xaddress", &self.address_xaddress);
        d.field("amount", &self.amount);
        d.field("amount_paid", &self.amount_paid);
        d.field("amount_remaining", &self.amount_remaining);
        d.field("confirmations", &self.confirmations);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("destination_tag", &self.destination_tag);
        d.field("expired_at", &self.expired_at);
        d.field("is_final", &self.is_final);
        d.field("is_multi", &self.is_multi);
        d.field("memo", &self.memo);
        d.field("method_adjustment", &self.method_adjustment);
        d.field("method_adjustment_bps", &self.method_adjustment_bps);
        d.field("network", &self.network);
        d.field("network_surcharge", &self.network_surcharge);
        d.field("network_surcharge_bps", &self.network_surcharge_bps);
        d.field("order_id", &self.order_id);
        d.field("payer_amount", &self.payer_amount);
        d.field("payer_currency", &self.payer_currency);
        d.field("rate_expires_at", &self.rate_expires_at);
        d.field("required_confirmations", &self.required_confirmations);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("url", &self.url);
        d.field("url_return", &self.url_return);
        d.field("url_success", &self.url_success);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct QrRequest {
    /// Произвольный адрес для рендера в QR-код (PNG как data:-URI).
    pub address: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for QrRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "QrRequest");
        d.field("address", &self.address);
        d.extra(&self.extra);
        d.finish()
    }
}

impl QrRequest {
    /// A `QrRequest` with the required fields; the optional ones start as `None`.
    pub fn new(address: impl Into<String>) -> Self {
        Self {
            address: address.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ReferralInfoResult {
    /// Реферальный код мерчанта.
    pub code: String,
    /// Заработано по активам, десятичными строками.
    pub earnings_by_asset: BTreeMap<String, String>,
    /// Реферальная ссылка (или сам код, если публичный адрес не настроен).
    pub link: String,
    /// Сколько мерчантов приглашено.
    pub referred_count: i64,
    /// Доля нашей комиссии по месяцам, в базисных пунктах.
    pub tier_bps: Vec<i64>,
    /// То же за скользящие 7 дней.
    pub week: ReferralWeek,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ReferralInfoResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ReferralInfoResult");
        d.field("code", &self.code);
        d.field("earnings_by_asset", &self.earnings_by_asset);
        d.field("link", &self.link);
        d.field("referred_count", &self.referred_count);
        d.field("tier_bps", &self.tier_bps);
        d.field("week", &self.week);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ReferralWeek {
    /// Заработано за 7 дней по активам, десятичными строками.
    pub earnings_by_asset: BTreeMap<String, String>,
    /// Приглашено за 7 дней.
    pub referred_count: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ReferralWeek {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ReferralWeek");
        d.field("earnings_by_asset", &self.earnings_by_asset);
        d.field("referred_count", &self.referred_count);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RefundBatchItem {
    /// Необязательный ключ идемпотентности возврата: различает два разных возврата с одинаковыми
    /// (платёж, адрес, сумма); повтор с тем же значением дедуплицируется. Это не order_id.
    pub reference: String,
    /// Адрес назначения возврата. По умолчанию — payer_address платежа; обязателен только для
    /// Bitcoin/UTXO.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address: Option<String>,
    /// Частичная сумма. По умолчанию — вся полученная.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount: Option<Money>,
    /// Профинансировать возврат конвертацией баланса: только USDT → валюта платежа. Нужен, когда
    /// монета платежа уже сведена автообменом.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from_currency: Option<String>,
    /// Сеть.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Ваша ссылка на заказ платежа. Нужен uuid или order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Идентификатор платежа. Нужен uuid или order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RefundBatchItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RefundBatchItem");
        d.field("reference", &self.reference);
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("from_currency", &self.from_currency);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RefundBatchRequest {
    /// Массив от 1 до 5000 элементов — те же поля, что у POST /v1/payment/refund; у каждого
    /// элемента обязательны reference (ключ идемпотентности) и uuid либо order_id платежа.
    pub refunds: Vec<RefundBatchItem>,
    /// Что делать при ошибке элемента: continue (по умолчанию) — обрабатывать остальные; stop —
    /// прекратить обработку после первой ошибки.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub on_error: Option<BatchOnError>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RefundBatchRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RefundBatchRequest");
        d.field("refunds", &self.refunds);
        d.field("on_error", &self.on_error);
        d.extra(&self.extra);
        d.finish()
    }
}

impl RefundBatchRequest {
    /// A `RefundBatchRequest` with the required fields; the optional ones start as `None`.
    pub fn new(refunds: Vec<RefundBatchItem>) -> Self {
        Self {
            refunds,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RefundFeeResult {
    /// true — проект задал настройку сам; false — действует умолчание шлюза.
    pub configured: bool,
    /// Действующее значение: настройка проекта, а без неё — умолчание шлюза.
    pub fee_on_customer: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RefundFeeResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RefundFeeResult");
        d.field("configured", &self.configured);
        d.field("fee_on_customer", &self.fee_on_customer);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RefundRequest {
    /// Адрес назначения возврата. По умолчанию — payer_address платежа; обязателен только для
    /// Bitcoin/UTXO.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address: Option<String>,
    /// Частичная сумма. По умолчанию — вся полученная.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount: Option<Money>,
    /// Профинансировать возврат конвертацией баланса: только USDT → валюта платежа. Нужен, когда
    /// монета платежа уже сведена автообменом.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub from_currency: Option<String>,
    /// Сеть.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Ваша ссылка на заказ платежа. Нужен uuid или order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Необязательный ключ идемпотентности возврата: различает два разных возврата с одинаковыми
    /// (платёж, адрес, сумма); повтор с тем же значением дедуплицируется. Это не order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reference: Option<String>,
    /// Идентификатор платежа. Нужен uuid или order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RefundRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RefundRequest");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("from_currency", &self.from_currency);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("reference", &self.reference);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RegisterWebhookRequest {
    /// HTTPS-URL коллбэка. SSRF-проверка: приватные и локальные адреса запрещены.
    pub url: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RegisterWebhookRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RegisterWebhookRequest");
        d.field("url", &self.url);
        d.extra(&self.extra);
        d.finish()
    }
}

impl RegisterWebhookRequest {
    /// A `RegisterWebhookRequest` with the required fields; the optional ones start as `None`.
    pub fn new(url: impl Into<String>) -> Self {
        Self {
            url: url.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RegisterWebhookResult {
    /// Идентификатор эндпоинта.
    pub endpoint_id: String,
    /// Зарегистрированный URL коллбэка.
    pub url: String,
    /// Секрет подписи — только в ответе на ПЕРВУЮ регистрацию, показывается один раз; при смене URL
    /// его нет (потеряли — перевыпустите: /v1/webhooks/rotate-secret).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub secret: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RegisterWebhookResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RegisterWebhookResult");
        d.field("endpoint_id", &self.endpoint_id);
        d.field("url", &self.url);
        d.field("secret", &self.secret);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ReplayRequest {
    /// Идентификатор доставки из GET /v1/sandbox/webhooks.
    pub delivery_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ReplayRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ReplayRequest");
        d.field("delivery_id", &self.delivery_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl ReplayRequest {
    /// A `ReplayRequest` with the required fields; the optional ones start as `None`.
    pub fn new(delivery_id: impl Into<String>) -> Self {
        Self {
            delivery_id: delivery_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ReplayResult {
    /// Идентификатор доставки, как передан.
    pub delivery_id: String,
    /// Всегда true: доставка поставлена в очередь; иначе — ошибка.
    pub ok: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ReplayResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ReplayResult");
        d.field("delivery_id", &self.delivery_id);
        d.field("ok", &self.ok);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RequeueWebhookDeliveryRequest {
    /// Идентификатор доставки из журнала (POST /v1/webhooks/deliveries).
    pub id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RequeueWebhookDeliveryRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RequeueWebhookDeliveryRequest");
        d.field("id", &self.id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl RequeueWebhookDeliveryRequest {
    /// A `RequeueWebhookDeliveryRequest` with the required fields; the optional ones start as `None`.
    pub fn new(id: impl Into<String>) -> Self {
        Self {
            id: id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RequeueWebhookDeliveryResult {
    /// Идентификатор доставки.
    pub id: String,
    /// true — этот вызов вернул доставку в очередь; false — она уже была в очереди или доставлена
    /// (повтор вызова ничего не меняет).
    pub ok: bool,
    /// Статус доставки после вызова: pending — снова в очереди; delivered — уже доставлена,
    /// повторять нечего.
    pub status: WebhookDeliveryStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RequeueWebhookDeliveryResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RequeueWebhookDeliveryResult");
        d.field("id", &self.id);
        d.field("ok", &self.ok);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResetResult {
    /// Сколько балансов (по активам) обнулено компенсирующей проводкой.
    pub balances_zeroed: i64,
    /// Сколько открытых счетов отменено.
    pub invoices_cancelled: i64,
    /// Сколько профинансированных выплатных ссылок отменено (резерв вернулся до обнуления).
    pub payout_links_cancelled: i64,
    /// Сколько профинансированных ссылок отменить не удалось — их резерв остался.
    pub payout_links_left: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ResetResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ResetResult");
        d.field("balances_zeroed", &self.balances_zeroed);
        d.field("invoices_cancelled", &self.invoices_cancelled);
        d.field("payout_links_cancelled", &self.payout_links_cancelled);
        d.field("payout_links_left", &self.payout_links_left);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResolveAcceptResult {
    /// Сколько оставлено мерчанту — всё, что пришло.
    pub amount_kept: String,
    /// Валюта оплаты.
    pub currency: String,
    /// Номер заказа мерчанта.
    pub order_id: String,
    /// Идентификатор платежа.
    pub payment_uuid: String,
    /// Принятое решение: accepted.
    pub resolution: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ResolveAcceptResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ResolveAcceptResult");
        d.field("amount_kept", &self.amount_kept);
        d.field("currency", &self.currency);
        d.field("order_id", &self.order_id);
        d.field("payment_uuid", &self.payment_uuid);
        d.field("resolution", &self.resolution);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResolveRefundResult {
    /// Адрес получателя.
    pub address: String,
    /// Сумма выплаты в валюте currency, списанная с вашего баланса.
    pub amount: Money,
    /// true — выплата ждёт подтверждения (внутренние сценарии; по API-ключу всегда false).
    pub approval_required: bool,
    /// Удержанная сетевая комиссия, в валюте выплаты. 0 — комиссию поглотил шлюз.
    pub commission: Money,
    /// Время создания (ISO 8601).
    pub created_at: String,
    /// Код валюты выплаты.
    pub currency: String,
    /// Подписанная ссылка на PDF-чек этой операции — открывается без API-ключа, можно вложить в
    /// письмо или отдать получателю. Пусто, если генерация документов не включена.
    pub document_url: String,
    /// Кто заплатил сетевую комиссию: gateway — шлюз поглотил её (commission = 0); merchant — сумма
    /// списания увеличена на комиссию, получатель получает запрошенное целиком (is_subtract=true,
    /// выплатная ссылка с fee_bearer=merchant); recipient — комиссия удержана из выплаты,
    /// получателю приходит меньше запрошенного.
    pub fee_bearer: PayoutFeeBearer,
    /// true — статус финальный (confirmed / failed / cancelled).
    pub is_final: bool,
    /// true — это возврат платежа, а не обычная выплата.
    pub is_refund: bool,
    /// Тег/мемо назначения, переданный при создании (TON Jetton, memo-биржи). Пусто — без мемо.
    pub memo: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Сколько реально уходит получателю на адрес: amount − commission.
    pub payer_amount: Money,
    /// Принятое решение: refunded.
    pub resolution: String,
    /// api (через интеграцию) | manual (из кабинета).
    pub source: PayoutSource,
    /// Статус выплаты: pending (создана, ждёт) | approved (одобрена) | awaiting_cosign (ждёт второй
    /// подписи) | broadcasting (отправляется) | sent (отправлена, ждёт подтверждений) | confirmed
    /// (подтверждена — готово) | failed | cancelled. Значение можно передать обратно в фильтр
    /// истории как есть.
    pub status: PayoutStatus,
    /// Хеш транзакции в блокчейне (появляется после отправки).
    pub txid: String,
    /// Время последнего изменения (ISO 8601).
    pub updated_at: String,
    /// Идентификатор выплаты.
    pub uuid: String,
    /// Ваш номер (reference) выплаты. У возврата — null: возврат не имеет вашего идентификатора,
    /// см. payment_order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Ваш order_id платежа, по которому сделан возврат (null у обычной выплаты). У возврата
    /// собственного order_id нет — он приходит null, а сверять возврат с заказом нужно по этому
    /// полю.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payment_order_id: Option<String>,
    /// Идентификатор возвращаемого платежа (null, если это не возврат).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_for: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ResolveRefundResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ResolveRefundResult");
        d.field("address", &self.address);
        d.field("amount", &self.amount);
        d.field("approval_required", &self.approval_required);
        d.field("commission", &self.commission);
        d.field("created_at", &self.created_at);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("fee_bearer", &self.fee_bearer);
        d.field("is_final", &self.is_final);
        d.field("is_refund", &self.is_refund);
        d.field("memo", &self.memo);
        d.field("network", &self.network);
        d.field("payer_amount", &self.payer_amount);
        d.field("resolution", &self.resolution);
        d.field("source", &self.source);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("updated_at", &self.updated_at);
        d.field("uuid", &self.uuid);
        d.field("order_id", &self.order_id);
        d.field("payment_order_id", &self.payment_order_id);
        d.field("refund_for", &self.refund_for);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResolveRequest {
    /// accept — принять частичную оплату, refund — вернуть плательщику.
    pub action: String,
    /// Только для refund: адрес возврата. По умолчанию — записанный payer_address платежа; если он
    /// пуст (Bitcoin/UTXO), адрес обязателен, иначе refund.no_address.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address: Option<String>,
    /// Только для refund: сеть возврата, по умолчанию — сеть платежа.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Ваш идентификатор платежа.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Только для refund: ваш ключ дедупликации возврата.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub reference: Option<String>,
    /// UUID платежа. Нужен uuid или order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for ResolveRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "ResolveRequest");
        d.field("action", &self.action);
        d.field("address", &self.address);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("reference", &self.reference);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

impl ResolveRequest {
    /// A `ResolveRequest` with the required fields; the optional ones start as `None`.
    pub fn new(action: impl Into<String>) -> Self {
        Self {
            action: action.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct RotateWebhookSecretResult {
    /// Идентификатор эндпоинта.
    pub endpoint_id: String,
    /// До этого момента доставки дополнительно подписываются старым секретом
    /// (X-Webhook-Signature-Prev), RFC 3339 UTC.
    pub previous_secret_valid_until: String,
    /// Новый секрет подписи — показывается только здесь.
    pub secret: String,
    /// URL коллбэка.
    pub url: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for RotateWebhookSecretResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "RotateWebhookSecretResult");
        d.field("endpoint_id", &self.endpoint_id);
        d.field(
            "previous_secret_valid_until",
            &self.previous_secret_valid_until,
        );
        d.field("secret", &self.secret);
        d.field("url", &self.url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SandboxDelivery {
    /// Сделано попыток.
    pub attempts: i64,
    /// Когда поставлена, RFC 3339 UTC.
    pub created_at: String,
    /// Событие в теле.
    pub event_type: String,
    /// Идентификатор доставки (для replay).
    pub id: String,
    /// Ошибка последней попытки; пусто, если её не было.
    pub last_error: String,
    /// Тело вебхука ровно так, как оно подписано и отправлено.
    pub payload: serde_json::Value,
    /// Состояние доставки.
    pub status: WebhookDeliveryStatus,
    /// Последнее изменение, RFC 3339 UTC.
    pub updated_at: String,
    /// Куда доставляется.
    pub url: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SandboxDelivery {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SandboxDelivery");
        d.field("attempts", &self.attempts);
        d.field("created_at", &self.created_at);
        d.field("event_type", &self.event_type);
        d.field("id", &self.id);
        d.field("last_error", &self.last_error);
        d.field("payload", &self.payload);
        d.field("status", &self.status);
        d.field("updated_at", &self.updated_at);
        d.field("url", &self.url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SandboxDeliveryList {
    /// Записи этой страницы.
    pub items: Vec<SandboxDelivery>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SandboxDeliveryList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SandboxDeliveryList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SandboxOnboardResult {
    /// Ключ API мерчанта.
    pub api_key: OnboardKey,
    /// true — dev store создан сейчас; false — уже был, секрет ключа пуст.
    pub created: bool,
    /// Мерчант.
    pub merchant_id: String,
    /// Первый проект мерчанта.
    pub project_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SandboxOnboardResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SandboxOnboardResult");
        d.field("api_key", &self.api_key);
        d.field("created", &self.created);
        d.field("merchant_id", &self.merchant_id);
        d.field("project_id", &self.project_id);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SendEmailRequest {
    /// Кому отправить. По умолчанию — payer_email, заданный у платежа.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub email: Option<String>,
    /// Ваша ссылка на заказ.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Идентификатор платежа в Oblodai. Нужен uuid или order_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SendEmailRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SendEmailRequest");
        d.field("email", &self.email);
        d.field("order_id", &self.order_id);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SendEmailResult {
    /// Кому ушло письмо.
    pub email: String,
    /// Письмо поставлено в очередь отправки; неудача отвечает ошибкой.
    pub ok: bool,
    /// Идентификатор платежа.
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SendEmailResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SendEmailResult");
        d.field("email", &self.email);
        d.field("ok", &self.ok);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetAccuracyRequest {
    /// Включить/выключить допуск
    pub enabled: bool,
    /// Допуск в процентах, 1–5. Обязателен при enabled: true; при enabled: false игнорируется
    /// (сбрасывается в 0). Кэп 5 %
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub accuracy_percent: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetAccuracyRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetAccuracyRequest");
        d.field("enabled", &self.enabled);
        d.field("accuracy_percent", &self.accuracy_percent);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetAccuracyRequest {
    /// A `SetAccuracyRequest` with the required fields; the optional ones start as `None`.
    pub fn new(enabled: bool) -> Self {
        Self {
            enabled,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetAutoConvertRequest {
    /// Монета, в которую сводится выручка (стейбл). Проверяется на возможность ликвидации при
    /// сохранении.
    pub target: String,
    /// Выключатель приказа целиком. Не передан — считается включённым.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// Пол одной конвертации в долларах, десятичной строкой; пусто — умолчание процесса ($10). Ниже
    /// него спред съедает больше, чем сводит.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_amount: Option<String>,
    /// Режим зачисления: "economy" — заявка в партию казначейской ликвидации, зачисляется факт
    /// исполнения (комиссия минимальная); "instant" — мгновенно по спред-курсу. Не передан —
    /// instant: автообмен включают ради мгновенного зачисления, а ждать партию — осознанный выбор.
    /// Иное значение — 400 request.invalid_mode.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub mode: Option<AutoConvertMode>,
    /// Монеты, которые сводить. Пусто — приказ есть, но не включён ни для чего.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sources: Option<Vec<String>>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetAutoConvertRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetAutoConvertRequest");
        d.field("target", &self.target);
        d.field("enabled", &self.enabled);
        d.field("min_amount", &self.min_amount);
        d.field("mode", &self.mode);
        d.field("sources", &self.sources);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetAutoConvertRequest {
    /// A `SetAutoConvertRequest` with the required fields; the optional ones start as `None`.
    pub fn new(target: impl Into<String>) -> Self {
        Self {
            target: target.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetAutoRefundRequest {
    /// Возвращать излишек при переплате (paid_over)
    pub overpay: bool,
    /// Возвращать средства при истёкшей недоплате (wrong_amount)
    pub underpay: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetAutoRefundRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetAutoRefundRequest");
        d.field("overpay", &self.overpay);
        d.field("underpay", &self.underpay);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetAutoRefundRequest {
    /// A `SetAutoRefundRequest` with the required fields; the optional ones start as `None`.
    pub fn new(overpay: bool, underpay: bool) -> Self {
        Self {
            overpay,
            underpay,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetDiscountRequest {
    /// Процент, от -99 до 99. Плюс — скидка, минус — наценка
    pub discount_percent: i64,
    /// Валюта. Пусто = глобальный дефолт для всех монет
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency: Option<String>,
    /// Сеть. Пусто = любая сеть данной валюты
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetDiscountRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetDiscountRequest");
        d.field("discount_percent", &self.discount_percent);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetDiscountRequest {
    /// A `SetDiscountRequest` with the required fields; the optional ones start as `None`.
    pub fn new(discount_percent: i64) -> Self {
        Self {
            discount_percent,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetPaymentFeeRequest {
    /// Доля НАШЕЙ комиссии, которую платит покупатель: 0 — платит мерчант (как сейчас), 100 —
    /// платит покупатель, счёт выставляется с наценкой. Действует на счета, созданные ПОСЛЕ
    /// изменения.
    pub payer_pays_percent: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetPaymentFeeRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetPaymentFeeRequest");
        d.field("payer_pays_percent", &self.payer_pays_percent);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetPaymentFeeRequest {
    /// A `SetPaymentFeeRequest` with the required fields; the optional ones start as `None`.
    pub fn new(payer_pays_percent: i64) -> Self {
        Self {
            payer_pays_percent,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetPayoutFeeRequest {
    /// true — сетевую комиссию платит получатель (получает меньше); false — комиссию несёт мерчант
    pub fee_on_recipient: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetPayoutFeeRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetPayoutFeeRequest");
        d.field("fee_on_recipient", &self.fee_on_recipient);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetPayoutFeeRequest {
    /// A `SetPayoutFeeRequest` with the required fields; the optional ones start as `None`.
    pub fn new(fee_on_recipient: bool) -> Self {
        Self {
            fee_on_recipient,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetRefundFeeRequest {
    /// true — клиент получает net (комиссию платит клиент); false — мерчант платит комиссию, клиент
    /// получает gross
    pub fee_on_customer: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetRefundFeeRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetRefundFeeRequest");
        d.field("fee_on_customer", &self.fee_on_customer);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SetRefundFeeRequest {
    /// A `SetRefundFeeRequest` with the required fields; the optional ones start as `None`.
    pub fn new(fee_on_customer: bool) -> Self {
        Self {
            fee_on_customer,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetWebhookActiveRequest {
    /// true — доставка возобновляется, false — прекращается (очередь по этому проекту больше не
    /// наполняется).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub active: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetWebhookActiveRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetWebhookActiveRequest");
        d.field("active", &self.active);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SetWebhookActiveResult {
    /// Включена ли теперь доставка.
    pub active: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SetWebhookActiveResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SetWebhookActiveResult");
        d.field("active", &self.active);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SimulateDepositRequest {
    /// UUID тестового счёта, который «оплачивается».
    pub invoice_id: String,
    /// Сумма в валюте счёта; пусто — оплатить ровно сколько нужно, иное — способ получить
    /// недо/переплату.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub amount: Option<String>,
    /// С каким числом подтверждений пришёл депозит; 0 — полностью подтверждён; меньше требуемого —
    /// способ проверить переход pending→confirmed (повторите тот же txid с большим числом).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub confirmations: Option<i64>,
    /// Повтор того же txid проверяет вашу идемпотентность; пусто — новый txid.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub txid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SimulateDepositRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SimulateDepositRequest");
        d.field("invoice_id", &self.invoice_id);
        d.field("amount", &self.amount);
        d.field("confirmations", &self.confirmations);
        d.field("txid", &self.txid);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SimulateDepositRequest {
    /// A `SimulateDepositRequest` with the required fields; the optional ones start as `None`.
    pub fn new(invoice_id: impl Into<String>) -> Self {
        Self {
            invoice_id: invoice_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SimulateDepositResult {
    /// Сумма депозита в валюте счёта.
    pub amount: Money,
    /// С каким числом подтверждений депозит отдан конвейеру.
    pub confirmations: i64,
    /// Оплачиваемый тестовый счёт.
    pub invoice_id: String,
    /// Транзакция депозита (с префиксом песочницы); повтор того же txid проверяет вашу
    /// идемпотентность.
    pub txid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SimulateDepositResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SimulateDepositResult");
        d.field("amount", &self.amount);
        d.field("confirmations", &self.confirmations);
        d.field("invoice_id", &self.invoice_id);
        d.field("txid", &self.txid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SoFSubmitRequest {
    /// Откуда средства.
    pub origin: String,
    /// Как связаться для уточнений.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub contact: Option<String>,
    /// Чем подтверждается: ссылки на выписки, идентификаторы транзакций.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub evidence: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SoFSubmitRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SoFSubmitRequest");
        d.field("origin", &self.origin);
        d.field("contact", &self.contact);
        d.field("evidence", &self.evidence);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SoFSubmitRequest {
    /// A `SoFSubmitRequest` with the required fields; the optional ones start as `None`.
    pub fn new(origin: impl Into<String>) -> Self {
        Self {
            origin: origin.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SoFSubmitted {
    /// Анкета принята к рассмотрению; это не решение о разблокировке.
    pub accepted: bool,
    /// Статус анкеты после приёма — completed.
    pub status: SoFStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SoFSubmitted {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SoFSubmitted");
        d.field("accepted", &self.accepted);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SoFView {
    /// Срок ссылки вышел — анкету уже не принять.
    pub expired: bool,
    /// Статус анкеты.
    pub status: SoFStatus,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SoFView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SoFView");
        d.field("expired", &self.expired);
        d.field("status", &self.status);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitConfigRequest {
    /// На сколько секунд откладывать расчёт по сплитам; диапазон 0–7776000 (до 90 суток). 0 —
    /// отправлять доли сразу: риск невозможности возврата берёте на себя.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub refund_hold_seconds: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitConfigRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitConfigRequest");
        d.field("refund_hold_seconds", &self.refund_hold_seconds);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitConfigView {
    /// На сколько секунд откладывается расчёт по сплитам после оплаты; 0 — доли уходят сразу.
    pub refund_hold_seconds: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitConfigView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitConfigView");
        d.field("refund_hold_seconds", &self.refund_hold_seconds);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRecipientOptInRequest {
    /// Разрешить другим мерчантам направлять доли сплитов на ваш баланс. true — включить приём,
    /// false — выключить (новые правила на вас перестанут создаваться; уже созданные продолжают
    /// исполняться).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRecipientOptInRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRecipientOptInRequest");
        d.field("enabled", &self.enabled);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRecipientOptInView {
    /// true — другие мерчанты могут направлять доли на ваш баланс.
    pub enabled: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRecipientOptInView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRecipientOptInView");
        d.field("enabled", &self.enabled);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRuleCreated {
    /// Сохранённая доля в процентах, два знака после точки.
    pub percent: Money,
    /// Идентификатор правила.
    pub rule_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRuleCreated {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRuleCreated");
        d.field("percent", &self.percent);
        d.field("rule_id", &self.rule_id);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRuleDeleteRequest {
    /// Идентификатор правила из POST /v1/split/rule или списка.
    pub rule_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRuleDeleteRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRuleDeleteRequest");
        d.field("rule_id", &self.rule_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SplitRuleDeleteRequest {
    /// A `SplitRuleDeleteRequest` with the required fields; the optional ones start as `None`.
    pub fn new(rule_id: impl Into<String>) -> Self {
        Self {
            rule_id: rule_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRuleDeleted {
    /// Правило удалено; неудача отвечает ошибкой.
    pub ok: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRuleDeleted {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRuleDeleted");
        d.field("ok", &self.ok);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRuleRequest {
    /// Доля от каждого платежа, строкой: "10" = 10 %, "2.5" = 2.5 %. Больше 0 и не больше 100, шаг
    /// 0.01 %; сумма всех правил не может превышать 100 %.
    pub percent: String,
    /// Внешний криптоадрес партнёра; доля уходит реальной транзакцией в блокчейне — необратимо.
    /// Ровно один вариант получателя: либо address+network, либо merchant_id.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address: Option<String>,
    /// Идентификатор мерчанта-партнёра внутри Oblodai; доля движется по внутреннему учёту и при
    /// возврате отзывается обратно.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub merchant_id: Option<String>,
    /// Сеть адреса. Обязательна вместе с address.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Комментарий для себя (виден в списке правил).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub note: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRuleRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRuleRequest");
        d.field("percent", &self.percent);
        d.field("address", &self.address);
        d.field("merchant_id", &self.merchant_id);
        d.field("network", &self.network);
        d.field("note", &self.note);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SplitRuleRequest {
    /// A `SplitRuleRequest` with the required fields; the optional ones start as `None`.
    pub fn new(percent: impl Into<String>) -> Self {
        Self {
            percent: percent.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRuleView {
    /// Правило действует.
    pub active: bool,
    /// Комментарий из создания.
    pub note: String,
    /// Доля от каждого платежа в процентах.
    pub percent: Money,
    /// true — доля движется по внутреннему учёту и отзывается при возврате; false — уходит в
    /// блокчейн необратимо.
    pub reversible: bool,
    /// Идентификатор правила.
    pub rule_id: String,
    /// Внешний адрес партнёра; есть у внешнего получателя.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address: Option<String>,
    /// Мерчант-партнёр внутри Oblodai; есть у внутреннего получателя.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub merchant_id: Option<String>,
    /// Сеть внешнего адреса; есть у внешнего получателя.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRuleView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRuleView");
        d.field("active", &self.active);
        d.field("note", &self.note);
        d.field("percent", &self.percent);
        d.field("reversible", &self.reversible);
        d.field("rule_id", &self.rule_id);
        d.field("address", &self.address);
        d.field("merchant_id", &self.merchant_id);
        d.field("network", &self.network);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SplitRuleViewList {
    /// Записи этой страницы.
    pub items: Vec<SplitRuleView>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SplitRuleViewList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SplitRuleViewList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct StaticWalletView {
    /// Постоянный адрес для пополнений. На XRP — классический r-адрес ОБЩЕГО кошелька; пополнение
    /// обязано нести destination_tag. На XLM — G-адрес; пополнение обязано нести memo.
    pub address: String,
    /// true — кошелёк заблокирован: пополнения на этот адрес НЕ зачисляются (уходят в карантин
    /// оператору, без вебхука и без автовозврата). Публиковать такой адрес нельзя.
    pub blocked: bool,
    /// Валюта пополнений.
    pub currency: String,
    /// Подписанная ссылка на PDF-справку о реквизитах. Пусто, когда рендер документов выключен.
    pub document_url: String,
    /// Сеть блокчейна.
    pub network: String,
    /// Ваш идентификатор клиента, за которым закреплён адрес (часть тройки идемпотентности
    /// currency+network+order_id).
    pub order_id: String,
    /// Зарезервировано (обычно пусто).
    pub url: String,
    /// Идентификатор статического кошелька.
    pub uuid: String,
    /// Только XLM: адрес и memo одной строкой (muxed M…, SEP-23).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address_muxed: Option<String>,
    /// Только XRP: адрес и тег одной строкой (X-address, XLS-5).
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub address_xaddress: Option<String>,
    /// Только XRP: числовой destination tag этого кошелька — клиент обязан указывать его в каждом
    /// переводе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub destination_tag: Option<String>,
    /// Только XLM: числовой memo (тип ID) этого кошелька — клиент обязан указывать его в каждом
    /// переводе.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub memo: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for StaticWalletView {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "StaticWalletView");
        d.field("address", &self.address);
        d.field("blocked", &self.blocked);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("url", &self.url);
        d.field("uuid", &self.uuid);
        d.field("address_muxed", &self.address_muxed);
        d.field("address_xaddress", &self.address_xaddress);
        d.field("destination_tag", &self.destination_tag);
        d.field("memo", &self.memo);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SummaryAmount {
    /// Сумма в единицах монеты.
    pub amount: Money,
    /// Монета оплаты.
    pub asset: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SummaryAmount {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SummaryAmount");
        d.field("amount", &self.amount);
        d.field("asset", &self.asset);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SummaryRequest {
    /// Начало окна, включительно (RFC 3339).
    pub from: String,
    /// Конец окна, не включительно (RFC 3339).
    pub to: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SummaryRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SummaryRequest");
        d.field("from", &self.from);
        d.field("to", &self.to);
        d.extra(&self.extra);
        d.finish()
    }
}

impl SummaryRequest {
    /// A `SummaryRequest` with the required fields; the optional ones start as `None`.
    pub fn new(from: impl Into<String>, to: impl Into<String>) -> Self {
        Self {
            from: from.into(),
            to: to.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct SummaryResult {
    /// Выплат в работе прямо сейчас (статус не финальный), без возвратов; от окна не зависит.
    pub pending_payouts: i64,
    /// Оборот окна: оплаченное по оплаченным счетам (paid, paid_over), созданным в окне, — по
    /// монете оплаты, по алфавиту. Пусто — оплат не было.
    pub turnover: Vec<SummaryAmount>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for SummaryResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "SummaryResult");
        d.field("pending_payouts", &self.pending_payouts);
        d.field("turnover", &self.turnover);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TestWebhookKindRequest {
    /// Куда отправить пробное тело
    pub url_callback: String,
    /// Валюта в теле
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub currency: Option<String>,
    /// Сеть в теле
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub network: Option<String>,
    /// Ваш order_id, который попадёт в пробное тело события
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Статус в теле — только те, с которыми боевой вебхук этого вида действительно приходит
    /// (кошелёк — только paid); иначе 400 webhook.bad_status. По умолчанию paid (для выплаты —
    /// confirmed, для конвертации — completed)
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// UUID объекта (платежа, кошелька или выплаты), который попадёт в пробное тело события
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub uuid: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TestWebhookKindRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TestWebhookKindRequest");
        d.field("url_callback", &self.url_callback);
        d.field("currency", &self.currency);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("status", &self.status);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

impl TestWebhookKindRequest {
    /// A `TestWebhookKindRequest` with the required fields; the optional ones start as `None`.
    pub fn new(url_callback: impl Into<String>) -> Self {
        Self {
            url_callback: url_callback.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TestWebhookKindResult {
    /// Всегда true: тело доставлено.
    pub ok: bool,
    /// Тело подписано секретом endpoint'а проекта.
    pub signed: bool,
    /// HTTP-статус, которым ответил ваш endpoint.
    pub status_code: i64,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TestWebhookKindResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TestWebhookKindResult");
        d.field("ok", &self.ok);
        d.field("signed", &self.signed);
        d.field("status_code", &self.status_code);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TestWebhookRequest {
    /// Статус в теле. По умолчанию paid
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status: Option<String>,
    /// Куда отправить пробное тело. Не передан — доставка уходит на зарегистрированный endpoint
    /// проекта; без endpoint — ошибка webhook.no_endpoint. Подпись — секретом endpoint'а проекта, в
    /// том числе при явном url
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub url: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TestWebhookRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TestWebhookRequest");
        d.field("status", &self.status);
        d.field("url", &self.url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TestWebhookResult {
    /// Сколько длилась доставка, мс.
    pub duration_ms: i64,
    /// Доставка состоялась (endpoint ответил, любым статусом).
    pub ok: bool,
    /// Тело подписано секретом endpoint'а проекта.
    pub signed: bool,
    /// Куда ушло пробное тело.
    pub url: String,
    /// Почему доставка не состоялась; только при ok=false.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub error: Option<String>,
    /// HTTP-статус ответа endpoint'а; только при ok=true.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub status_code: Option<i64>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TestWebhookResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TestWebhookResult");
        d.field("duration_ms", &self.duration_ms);
        d.field("ok", &self.ok);
        d.field("signed", &self.signed);
        d.field("url", &self.url);
        d.field("error", &self.error);
        d.field("status_code", &self.status_code);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransferBatchItem {
    /// Сумма перевода в currency.
    pub amount: Money,
    /// Код валюты (криптовалюта).
    pub currency: String,
    /// Ключ идемпотентности: повтор с тем же order_id — no-op; в батче переводов обязателен.
    pub order_id: String,
    /// Платформенный user id получателя (UUID, не username); username резолвится в id через
    /// публичный профиль кабинета /public/users/{username}.
    pub to_user_id: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TransferBatchItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TransferBatchItem");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("order_id", &self.order_id);
        d.field("to_user_id", &self.to_user_id);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransferBatchRequest {
    /// Массив от 1 до 5000 элементов — те же поля, что у POST /v1/transfer/to-user; у каждого
    /// элемента обязательны order_id (ключ идемпотентности) и to_user_id (UUID пользователя).
    pub transfers: Vec<TransferBatchItem>,
    /// Что делать при ошибке элемента: continue (по умолчанию) — обрабатывать остальные; stop —
    /// прекратить обработку после первой ошибки.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub on_error: Option<BatchOnError>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TransferBatchRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TransferBatchRequest");
        d.field("transfers", &self.transfers);
        d.field("on_error", &self.on_error);
        d.extra(&self.extra);
        d.finish()
    }
}

impl TransferBatchRequest {
    /// A `TransferBatchRequest` with the required fields; the optional ones start as `None`.
    pub fn new(transfers: Vec<TransferBatchItem>) -> Self {
        Self {
            transfers,
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransferRequest {
    /// Сумма перевода в currency.
    pub amount: Money,
    /// Код валюты (криптовалюта).
    pub currency: String,
    /// Ключ идемпотентности: повтор с тем же order_id — no-op. Настоятельно передавайте всегда,
    /// иначе повтор запроса при сетевом таймауте создаст второй перевод.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TransferRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TransferRequest");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("order_id", &self.order_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl TransferRequest {
    /// A `TransferRequest` with the required fields; the optional ones start as `None`.
    pub fn new(amount: impl Into<Money>, currency: impl Into<String>) -> Self {
        Self {
            amount: amount.into(),
            currency: currency.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransferResult {
    /// Сумма перевода.
    pub amount: Money,
    /// Актив перевода.
    pub currency: String,
    /// Ссылка на PDF-документ перевода; пусто, если документы выключены.
    pub document_url: String,
    /// Получатель — пользователь личного кошелька.
    pub to_user_id: String,
    /// Идентификатор проводки перевода.
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TransferResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TransferResult");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("document_url", &self.document_url);
        d.field("to_user_id", &self.to_user_id);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransferToPersonalResult {
    /// Сумма перевода.
    pub amount: Money,
    /// Актив перевода.
    pub currency: String,
    /// Направление: to_personal.
    pub direction: String,
    /// Ссылка на PDF-документ перевода; пусто, если документы выключены.
    pub document_url: String,
    /// Баланс личного кошелька владельца после перевода.
    pub personal_balance: String,
    /// Идентификатор проводки перевода.
    pub uuid: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TransferToPersonalResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TransferToPersonalResult");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("direction", &self.direction);
        d.field("document_url", &self.document_url);
        d.field("personal_balance", &self.personal_balance);
        d.field("uuid", &self.uuid);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct TransferToUserRequest {
    /// Сумма перевода в currency.
    pub amount: Money,
    /// Код валюты (криптовалюта).
    pub currency: String,
    /// Платформенный user id получателя (UUID, не username); username резолвится в id через
    /// публичный профиль кабинета /public/users/{username}.
    pub to_user_id: String,
    /// Ключ идемпотентности: повтор с тем же order_id — no-op; в батче переводов обязателен.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order_id: Option<String>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for TransferToUserRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "TransferToUserRequest");
        d.field("amount", &self.amount);
        d.field("currency", &self.currency);
        d.field("to_user_id", &self.to_user_id);
        d.field("order_id", &self.order_id);
        d.extra(&self.extra);
        d.finish()
    }
}

impl TransferToUserRequest {
    /// A `TransferToUserRequest` with the required fields; the optional ones start as `None`.
    pub fn new(
        amount: impl Into<Money>,
        currency: impl Into<String>,
        to_user_id: impl Into<String>,
    ) -> Self {
        Self {
            amount: amount.into(),
            currency: currency.into(),
            to_user_id: to_user_id.into(),
            ..Default::default()
        }
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VRCSRequest {
    /// true — включить автоконвертацию волатильных поступлений в USDT, false — выключить; без поля
    /// — только прочитать текущее состояние.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub enabled: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for VRCSRequest {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "VRCSRequest");
        d.field("enabled", &self.enabled);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct VRCSResult {
    /// Включена ли автоконвертация волатильных поступлений в USDT.
    pub enabled: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for VRCSResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "VRCSResult");
        d.field("enabled", &self.enabled);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WalletQRResult {
    /// PNG QR-кода как data:-URI; "" — не удалось отрисовать.
    pub image: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for WalletQRResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "WalletQRResult");
        d.field("image", &self.image);
        d.extra(&self.extra);
        d.finish()
    }
}

/// Приходит, когда депозит на статический кошелёк зачислен.
#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WalletWebhook {
    /// Адрес кошелька, на который пришёл платёж.
    pub address: String,
    /// Код валюты зачисления.
    pub currency: String,
    /// Когда событие произошло, UTC с миллисекундами (ISO 8601).
    pub event_at: String,
    /// true — статус финальный.
    pub is_final: bool,
    /// Сеть блокчейна.
    pub network: String,
    /// Ваш order_id кошелька.
    pub order_id: String,
    /// Валюта, в которой заплатил плательщик (совпадает с currency).
    pub payer_currency: String,
    /// Зачисленная сумма депозита (десятичное число строкой).
    pub payment_amount: String,
    /// Глобальный номер события: в пределах одного объекта больший номер новее, меньший —
    /// опоздавшая доставка, её нужно отбросить. У репетиции (test: true) всегда 0.
    pub sequence: i64,
    /// Статус в словаре платежа; живой поток шлёт только paid.
    pub status: String,
    /// Хеш транзакции депозита.
    pub txid: String,
    /// Вид события: payment | payout | wallet | conversion — какое тело пришло.
    pub r#type: String,
    /// Идентификатор статического кошелька.
    pub uuid: String,
    /// Есть только у репетиции (/v1/test-webhook/*, /v1/payment/testing-webhook) и всегда true —
    /// внутри подписи. Боевое событие этого поля не несёт никогда: тело с test: true обработчик
    /// обязан игнорировать, даже если подпись верна.
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub test: Option<bool>,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for WalletWebhook {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "WalletWebhook");
        d.field("address", &self.address);
        d.field("currency", &self.currency);
        d.field("event_at", &self.event_at);
        d.field("is_final", &self.is_final);
        d.field("network", &self.network);
        d.field("order_id", &self.order_id);
        d.field("payer_currency", &self.payer_currency);
        d.field("payment_amount", &self.payment_amount);
        d.field("sequence", &self.sequence);
        d.field("status", &self.status);
        d.field("txid", &self.txid);
        d.field("type", &self.r#type);
        d.field("uuid", &self.uuid);
        d.field("test", &self.test);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WebhookDeliveryLogItem {
    /// Сделано попыток.
    pub attempts: i64,
    /// Почему доставка cancelled не будет отправлена; пусто у остальных статусов.
    pub cancel_reason: String,
    /// Когда поставлена, RFC 3339 UTC.
    pub created_at: String,
    /// Событие в теле.
    pub event_type: String,
    /// Идентификатор доставки.
    pub id: String,
    /// Ошибка последней попытки; пусто, если её не было.
    pub last_error: String,
    /// Глобальный номер события (тот же, что в теле).
    pub sequence: i64,
    /// Состояние доставки.
    pub status: WebhookDeliveryStatus,
    /// Последнее изменение, RFC 3339 UTC.
    pub updated_at: String,
    /// Куда доставляется.
    pub url: String,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for WebhookDeliveryLogItem {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "WebhookDeliveryLogItem");
        d.field("attempts", &self.attempts);
        d.field("cancel_reason", &self.cancel_reason);
        d.field("created_at", &self.created_at);
        d.field("event_type", &self.event_type);
        d.field("id", &self.id);
        d.field("last_error", &self.last_error);
        d.field("sequence", &self.sequence);
        d.field("status", &self.status);
        d.field("updated_at", &self.updated_at);
        d.field("url", &self.url);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WebhookDeliveryLogItemList {
    /// Записи этой страницы.
    pub items: Vec<WebhookDeliveryLogItem>,
    /// Блок пагинации.
    pub paginate: Pagination,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for WebhookDeliveryLogItemList {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "WebhookDeliveryLogItemList");
        d.field("items", &self.items);
        d.field("paginate", &self.paginate);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Default, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct WebhookResendResult {
    /// Всегда true: вебхук поставлен в очередь; неудача отвечает ошибкой.
    pub ok: bool,
    /// Fields this SDK version does not know yet; sent back as they are.
    #[serde(flatten)]
    pub extra: serde_json::Map<String, serde_json::Value>,
}

impl std::fmt::Debug for WebhookResendResult {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let mut d = ModelDebug::new(f, "WebhookResendResult");
        d.field("ok", &self.ok);
        d.extra(&self.extra);
        d.finish()
    }
}

#[derive(Clone, Debug, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(untagged)]
pub enum OnrampStatusResult {
    OnrampSessionView(OnrampSessionView),
    OnrampIdle(OnrampIdle),
    /// A shape this SDK version does not know yet.
    Unknown(serde_json::Value),
}

impl Default for OnrampStatusResult {
    fn default() -> Self {
        Self::Unknown(serde_json::Value::Null)
    }
}

#[derive(Clone, Debug, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(untagged)]
pub enum PayoutClaimInfo {
    PayoutClaimView(PayoutClaimView),
    PayoutClaimLockedView(PayoutClaimLockedView),
    /// A shape this SDK version does not know yet.
    Unknown(serde_json::Value),
}

impl Default for PayoutClaimInfo {
    fn default() -> Self {
        Self::Unknown(serde_json::Value::Null)
    }
}

#[derive(Clone, Debug, PartialEq, serde::Serialize, serde::Deserialize)]
#[serde(untagged)]
pub enum ResolveResult {
    ResolveRefundResult(ResolveRefundResult),
    ResolveAcceptResult(ResolveAcceptResult),
    /// A shape this SDK version does not know yet.
    Unknown(serde_json::Value),
}

impl Default for ResolveResult {
    fn default() -> Self {
        Self::Unknown(serde_json::Value::Null)
    }
}
